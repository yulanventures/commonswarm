# Master plan to v0.1.5 completion — rev 2

This plan is the record of the v0.1.5 rollout. Do not run `vercel deploy` or `supabase db push --linked` from it. The site publish command is `deploy/site/deploy.sh yulan-vps-1`. The current box release procedure for schema migrations and edge functions is `deploy/RELEASE-TO-BOX.md`.

> **Superseded in part by D-044, 2026-08-04:** the cross-owner fresh-session, zero-tool, and no-context
> canary below is retired. Its replacement proves delivery to the operator's existing worker and
> project context with factual provenance and the cross-owner confirmation steer.

Owner: ClaudeCswarm (relief lead)
Written: 2026-08-02 · **Revised 2026-08-02 after adversarial Fable review**
Supersedes the live-lane portions of `EXECUTION-ORDERS.md` and `CLAUDE-PAUSE-HANDOFF-2026-07-31.md`.
Those remain valid for operator decisions, safety doctrine, and history.

Inputs: `QM-COMPARATIVE-ANALYSIS.md` (2026-08-02, **recommendation A retracted — see §5**).

## Revision log (rev 1 → rev 2)

Ten findings from an independent adversarial review were accepted; all were verified by me against
the tree before adoption. Superseded lines are kept and marked **dead** per the corrections doctrine.

1. **CRITICAL — rev 1 claimed the Runtime A2 worktree was clean. It was not.** A live mutant
   (`this.isCredentialFailure = undefined`) was still applied at the moment rev 1 was written, so
   rev 1's measured-state table had a false row and its strongest completion claim was unfounded.
   The mutant has now been restored (`git checkout --`; status clean; 0 diff lines vs `ab1b240`).
   The superseded line — *"Runtime A2 `ab1b240`, clean, == upstream"* — is **dead** for the moment
   it was written. This is exactly the failure our doctrine names: I asserted a cleanliness I had
   verified an hour earlier and not re-measured.
2. **Stage 2a deleted.** It proposed building a test-reach checker we **already have**:
   `tests/p1-cli/test-gate-coverage.test.ts` asserts *"D-030: every test file is reached by an npm
   execution script"* with `unreachable == []`, and is named in the `npm test` literal list. The
   superseded line — *"our worst trap is unguarded, adopt QM's check"* — is **dead**. I
   pattern-matched from an external repo instead of enumerating our own suite.
3. **New Stage 0**: durably record the Grok substitution. Committed doctrine (`AGENTS.md:159`,
   D-033) requires **both** Grok and Gemini; the substitution exists only in gitignored scratchpad.
4. Explicit **two-arm cross-family review** added to Stages 1a, 1b, 4, and as a named step 7b.
5. **Lane Q (authenticated production QA) moved pre-freeze**, parallel from Stage 1.
6. Lane R and durable doc landing **pinned before Stage 6**; evidence committed **per stage**.
7. "Causal" downgraded to an honest characterization; A2 completion 85% → **~55%**.
8. The SHA-restart rule **scoped** to the frozen release candidate.
9. Reviewer-depth doctrine **deferred** to post-0.1.5 (it licenses shallower review at exactly the
   wrong moment).
10. Estimate corrected to **40–55 h**; Stage 12 priced; `test:uxtest` added to Stage 7.

---

## 0. The goal, unshrunk

> Execute, integrate, deploy, and production-verify the complete CommonSwarm MVP gap-closure plan:
> consolidate the authoritative deploy clone and durable dossier, quarantine and clean unsafe
> worktrees, run authenticated production QA and reconcile ledger/docs, measure and ship a safe
> second host adapter, design and preferably ship durable delivery acknowledgements, run cross-owner
> and longevity canaries, complete exact-SHA reviews and release/deployment evidence, then perform
> bounded worktree cleanup and a read-only branch audit.

Completion is a **released and production-verified v0.1.5**, not the first milestone.

## 1. Measured state (re-measured 2026-08-02, post-correction)

| Fact | Value | How measured |
|---|---|---|
| Lead branch | `lead7/mvp-release-0.1.5` @ `d7c0b1a`, clean, == upstream | `git rev-parse HEAD` / `@{upstream}` |
| Lead vs main | **56 ahead, 0 behind** → fast-forward is genuinely possible | `git rev-list --count` |
| `origin/main` | `20ade63` | `git rev-parse origin/main` |
| Runtime A2 | `ab1b240`, **clean as of the restore above** — re-measure before use | worktree probe |
| Server Phase B | `d972c3f`, clean, == upstream | worktree probe |
| Repo version | `0.1.4`; no `v0.1.5` tag | `package.json`, `git ls-remote --tags` |
| Production | `/download` 0.1.4 ×4; 9 routes + `/install.sh` 200; `/nope-*` 404 | live curl + negative control |
| DB slot | free, 0 processes | `ps aux` |
| Worktrees / branches | 32 / 57 local / 82 remote | `git worktree list`, `git branch [-r]` |
| Test reach | `npm test` = 26 literal paths, 26 unique; **44 test files, 0 unreached** | `package.json` + D-030 observer |

**Standing rule: re-measure every row before acting on it.** Rev 1's false row was produced by
trusting a measurement taken an hour earlier.

## 2. Non-negotiables (operator decisions — do not re-ask)

1. Deploy clone `/Users/yulanbot/Developer/Ridge.io/cloud-swarm`; integration branch
   `lead7/mvp-release-0.1.5`.
2. Legal activation, counsel, dates, DMCA, trademark: **deferred post-MVP**. Draft banners stay.
3. Never broadcast. Direct messages only. Never contact `AdvisorClaude2` except on full completion
   or a critical flaw the dossier cannot resolve.
4. Grok is credit-exhausted (see Stage 0). Claude/Codex supervise/review/integrate; DeepSeek
   V4-Flash or Kimi K3 implement via `pi-openrouter`; Gemini/AGY inverts. Never expose the key.
5. One writer per worktree. One exclusive DB lane. No builder self-approval.
6. **Any SHA movement after the Stage 6 freeze restarts exact + cross-family review of the release
   candidate.** *(Scoped in rev 2: pre-freeze landings on Lead do not restart already-completed
   per-lane reviews. Superseded unscoped line — "any SHA movement after review restarts review" —
   is **dead** as written.)*
7. No worktree or branch deletion until the final bounded pass. Remote deletion is operator-only.
8. Deploy is the operator's call. No CI; nothing deploys on push.

## 2b. Milestone review cadence (binding, added 2026-08-03)

Independent cross-family review at **stage boundaries**, not only when something feels wrong. Three
of these have already paid for themselves: the first rewrote this plan (rev 1 -> rev 2), the second
found that every D-036 arm had been delta-scoped to *test* files leaving ~1,932 lines of production
code unreviewed, and the third caught four defects in the Runtime C goal *before* dispatch — including
a repair instruction that could not work.

| # | Fires at | Question it must answer |
|---|---|---|
| M1 | plan written | is the plan itself sound? ✅ rev 1 -> rev 2 |
| M2 | integration merge complete | did anything get lost, and what is unreviewed? ✅ found the production-code gap |
| M3 | before dispatching a large lane | is the frozen goal correct *before* a worker burns on it? ✅ 4 defects caught |
| M4 | last build lane complete | is the tree ready to freeze? |
| M5 | after the full exact-SHA gate, before landing | is the release candidate actually releasable? |
| M6 | after production rollout | did production match what we proved locally? |

Rules that make these worth running:

- Review the **artifact**, not a summary of it — hand over paths, not prose.
- A review that returns no findings is a result; a review that returns only praise was mis-scoped.
- Findings go into the artifact they concern, with superseded lines kept and marked dead.
- **A milestone review is not a substitute for a D-036 arm**, and a D-036 arm is not a substitute for
  a milestone review. The first asks "is this plan right?", the second asks "is this diff right?".

Reserve them for genuine boundaries and for the case where several attempts at one problem have
failed. Running one per routine checkpoint dilutes the signal and wastes the budget.

## 3. Critical path

### Stage 0 — Record the review gate durably *(NEW — must precede any review)*

Committed doctrine says the required pair is **Grok + AGY/Gemini on the exact SHA**, and that
"Grok alone, Gemini alone, and Codex review are each insufficient." Grok's credit exhaustion and the
operator's substitution live **only in gitignored scratchpad** — `grep -rn "credit-exhaust" docs/`
returns nothing. Every acceptance in Stages 1–7 would therefore be executed against a committed gate
it does not satisfy, and a later reader applying our own doctrine would have to reject the release.
`V015-RELEASE-CHECKLIST.md` correction 6 already orders this and no stage did it.

- Commit a **D-036 ruling** in `docs/org/DEFECT-REGISTER.md` (D-035 is taken) recording: Grok
  credit-exhausted; the operator-authorized substitute gate (exact Codex/Claude review + Gemini/AGY
  or Kimi inversion); its scope and expiry.
- Supersede the `AGENTS.md` gate paragraph in place — **keep the old line, mark it dead**, cite
  D-036.
- Add the durable-by-default line (from QM) in the same commit.
- **Do NOT add reviewer-depth scaling** — deferred; see §5.
- Cost: ~30 min. Blocks: all reviews.

### Stage 1 — Accept the two moved candidates

**1a. Runtime A2 `ab1b240`** — *~55% done (was overstated as 85%).*
- ✅ Exact code review vs the frozen goal: typed-HTTP precedence, classifier-throw restoration,
  caller-abort precedence, name-only `AbortError`, exact caller-signal forwarding — all PASS.
- ✅ Gates re-run by me: `npm test` **331/331**, `check:tests`, `build`, `diff --check` clean,
  focused engine+runtime **38/38**. *(Counts are mine, not the worker's.)*
- ⚠️ **Control 1 must be re-run.** It produced a **hang**, not a printed failure. A hang proves the
  classifier is load-bearing for *completion*; it does **not** prove any assertion detects its
  absence — an unresolved promise hangs identically. The superseded characterization — *"causal"* —
  is **dead**. Re-run bounded (`--test-timeout`) to produce a clean red, per the D-030 precedent
  requiring printed discriminating failures.
- ⬜ Controls 2–3 bounded: runtime message-cancellation-before-credential; poster signal-forwarding
  removal. Restore byte-identically; prove zero residual processes; **re-measure `git status` after
  each** (rev 1's failure).
- ⬜ **Two-arm cross-family review on the exact SHA** per `EXECUTION-ORDERS.md` §1 rule 4 and
  Stage 0's substitute gate. The handoff's "a fresh cross-family review is desirable" is not the
  governing text; rule 4 is.
- ⬜ Merge into Lead, preserve Runtime B, re-enumerate the literal test list, run pure gates, push,
  record the new exact Lead SHA. Commit acceptance evidence to `docs/evidence/`.
- Cost: ~2 h.

**1b. Server Phase B `d972c3f`** — *not started. Highest-risk artifact in the tree.*
- Requires the **sole DB slot**; announce take and release.
- Code-inspect all four predecessor blockers — the predecessor's green tests did **not** clear its
  failure-path defects, so inspection is mandatory: (1) cancel **and await** the exact SQL query on
  pending-arm settlement; (2) one monotonic absolute deadline with bounded remaining-time sleeps;
  (3) retain, release, **and await** `retainedAttempt` on every pre-ready path; (4) preserve
  arbitrary primary values by **exact identity** (object/string/number/symbol/null/undefined).
- Causal mutant with a **printed** red; restore byte-for-byte; prove zero residual processes.
- ⬜ **Two-arm cross-family review on the exact SHA.** Evidence → `docs/evidence/`.
- Cost: ~3 h + exclusive DB.

### Stage 2 — ~~Test-reach check script~~ **DELETED**

**Dead.** We already have it: `tests/p1-cli/test-gate-coverage.test.ts` enumerates every
`tests/**/*.test.ts`, parses every `--test` script in `package.json`, and asserts `unreachable == []`
(D-030), hardened by five printed mutation controls. It is named in the `npm test` literal list, so
Stage 4's merge is **already gated** — drop `tests/claim-ledger-parse.test.ts` from the union and the
pure gate goes red.

Replacement, 5 minutes at Stage 4: **prove the existing observer still discriminates** — temporarily
remove one path from the literal list, watch `npm test` go red, restore.

### Stage 3 — Server Phase C

Blocked on 1b PASS. Bind the **accepted SHA** (not the branch name) into a frozen goal using
`SERVER-PHASE-C-PREFLIGHT-CORRECTIONS.md`. Owns only `tests/p1-server/command.test.ts`; its
temporary production mutant restored byte-for-byte. One Pi/DeepSeek worker, frozen complete packet,
no concurrent DB lane. Exact-review Phase C **and** its recharge-deletion mutant, plus the two-arm
review, before acceptance. Cost: ~3–4 h.

### Stage 4 — Server → Lead integration

Three-way merge; server descends `main@20ade63`, Lead has 56 later commits; delta 10 paths /
6,436 insertions. **Never wholesale-checkout the server tree over Lead.**
Known `package.json` conflict: preserve Lead's 26 paths, append `tests/claim-ledger-parse.test.ts`
exactly once → **27 unique**. Re-enumerate; run the D-030 discrimination control here.
Inspect design and generated/bundle paths individually. Pure/static gates, then DB suites serially
with zero-process controls between. **Two-arm review on the merge SHA** (6,436 insertions is the
largest blast radius in the plan). Cost: ~2.5 h + DB.

### Stage 5 — Runtime C, then Runtime D

Freeze C from the accepted integrated base. C owns only `src/listener/runtime.ts`, optional private
`src/listener/durable-runtime.ts`, narrow reducer changes in `supervisor.ts`, and
`tests/listener-runtime.test.ts`. Sequential checkpoints in one writer: config/modes/events →
journal/claim/retry/budget → effects/ACK/cancel/rollback/crash.
⬜ *(QM, small)* Check whether host switching is reachable at all; our `ListenerModel`
(`src/listener/types.ts:71`) has no teardown. If unreachable — likely, one host per listener
process — **do nothing** and record that.
D follows accepted C; owns only `src/cli.ts`, the permitted `src/cloud/delivery.ts` comment,
`tests/listener-cli-process.test.ts`, `tests/support/agent-receive-cli.test.ts`.
Exact-review + two-arm inversion each. Cost: ~6–8 h.

### Stage Q — Authenticated production QA *(∥ from Stage 1 — MOVED EARLIER)*

`EXECUTION-ORDERS.md` §3 makes this the lane that "validates the actual customer journey" and orders
it to start immediately. Rev 1 wrongly deferred it to post-deploy, where any fix would move a SHA
**after** the freeze and force a v0.1.6 respin. It contends for nothing on the critical path — Chrome
only, no DB slot, no working tree.

Run against **current production** in parallel with Stages 1–5: cold-browser magic link, workspace
create, own-agent onboarding/auto-close, roster identity, live feed ≤ 5 s, teammate invite
consumption, **pending-access clearing**, remove/revoke, 390×844 mobile, keyboard/a11y, cleanup.
Diagnose, do not fix. Findings land in the pre-freeze fix wave (Stage 5b) or are explicitly accepted
as shipping. Resolve QA-005 / QA-006 / QA-007 here. Evidence → `docs/evidence/`. Cost: ~4 h.

**Decision owed to the operator before Stage 6:** fix QA findings pre-freeze, or accept shipping
them in 0.1.5.

### Stage 6 — Version freeze *(proposed check-in)*

Reconcile the complete decision **set**, not the items. `npm version --no-git-tag-version 0.1.5`;
prove all three fields agree (`package.json.version`, `package-lock.json.version`,
`package-lock.json.packages[""].version`). Do **not** hand-edit generated `/download` surfaces, the
agent minimum-version copy, or `CLIENT_PROTOCOL_VERSION` — separate runtime surfaces.
Commit and freeze the exact SHA. **From here, any movement restarts exact + cross-family review.**
Cost: ~30 min.

### Stage 7 — Full exact-SHA gate (`V015-RELEASE-CHECKLIST.md` binding)

Pure/static: `npm test`, `check:tests`, `build`, `build:command-core`,
`git diff --exit-code -- supabase/functions/_shared/protocol.js`, `check:edge`.
Then, with a proven-free slot, **serially**: `db:start`, `db:reset`, `test:p1-cli`, `test:p1-local`,
`test:p1-server`, `check:edge`. Record exits, counts, elapsed, zero-process checks between each.
Then clean `site/dist` build (enumerate exactly 8 HTML routes), `npm --prefix site test`,
`scripts/build-release.sh`, `git diff --check`, `git status --short`.
**`npm run test:uxtest` — run it if the union affects its journey, else record explicitly as skipped
and unestablished** *(omitted from rev 1)*.
Also prove: release-bundle shebang; an **isolated copy** prints exactly `cswarm 0.1.5 (protocol
0.1.0)`; `shasum -a 256 -c` passes; root / `site/public` / built `site/dist` installers
byte-identical; four `/download` 0.1.5 surfaces each exactly once; browser-bundle sentinels.
~~Execute **12 recorded controls**; **0 of 10 domains has a recorded domain-valid control and all 10
are blind**~~ — **dead (2026-08-03):** the remaining-controls lane added seven exact controls and
closed six more domains. Execute the **22 recorded controls** in
`docs/design/2026-08-03-STAGE7-CAUSAL-CONTROL-REGISTER.md`. The register now credits **9 of 10**
required domains; claim one-winner remains blind after its exact mutant stayed green and triggered
the mandatory stop. Cost: ~4 h.

### Stage 7b — Release-candidate cross-family review *(NEW)*

The frozen 0.1.5 SHA gets its own two-arm review under the Stage 0 gate. Rev 1 said movement
"restarts cross-family review" but never scheduled the first one. Cost: ~1 h wall clock.

### Stage 8 — Land

Re-fetch and re-read remote `main`; stop on unexpected movement. Fast-forward the accepted SHA
(56 ahead / 0 behind confirms this is possible). **Never force-push.** Prove local == live remote.
Cost: ~30 min.

### Stage 9 — Staged production rollout *(operator's call)*

Order is load-bearing: **migration → command → backend controls → read capability → CLI tag/release/
assets → site last.** The site must never advertise 0.1.5 before its assets exist.
Before mutating, record the remote migration ledger, function versions/hashes, Vercel deployment,
v0.1.4 tag/assets/checksum, and public/backend controls.
The site publish command is `deploy/site/deploy.sh yulan-vps-1`. Verify the deployed page, never the source, pairing every must-be-absent grep with a must-be-present one on the same invocation.
**Start the 24 h and 7 d canaries here** so wall-clock runs in parallel. Cost: ~3 h.

### Stage 10 — Production verification

Public controls: 8 routes + `/install.sh` → 200, `/nope.sh` → 404; `/download` 0.1.5 surfaces;
`/start` non-empty backend meta, **zero** service-role markers; deployed installer matches root.
Authenticated journey re-run against 0.1.5 (Stage Q's script, now post-deploy).
Listener/delivery: same-owner ask/note, one-winner concurrent claim, restart, response-loss replay,
persist-before-ACK, revocation stop bound, credential absent from argv/env/status/logs, legacy
v0.1.4 cursor client still works. Accelerated credential rotation. Cost: ~5 h.

### Stage 11 — ⚠️ Blocked on the operator

Real two-human cross-owner canary `tlangridge` (mini) ↔ `Ridgeio` (**operator's laptop**): hostile
cross-owner ask, fresh session / zero tools / no context, same-owner control arm, revocation stop.
**Cannot be completed from the mini alone.** 30 d canary stays post-MVP and unestablished.

### Stage 12 — Reconciliation and cleanup *(last; irreversible)*

*Evidence is no longer batched here — each stage commits its own to `docs/evidence/` as produced,
because this handoff chain exists precisely because sessions die mid-plan.*
- **Before Stage 6:** land this plan + `QM-COMPARATIVE-ANALYSIS.md` durably (`scratchpad/` is
  gitignored); finish Lane R. Lane R is ∥ from Stage 1 but **must land before the freeze** — name
  its branch and pin it.
- Lane R content: close awaiting_review + landed actives against **main/production anchors, never
  branch names**; `TODO.md`, `CHARTER.md`, `DEFECT-REGISTER.md` (D-003/019/020/022/023/034);
  invite-spec checkboxes; post-MVP legal TODO block; correct the dossier's `ListenerHostAdapter` →
  `ListenerModel` naming error.
- Worktree cleanup (Lane W): 32 registered; re-verify `git cherry`/status per tree immediately
  before each removal; before/after enumeration is the evidence; legal worktree retained.
- Branch audit (Lane X): read-only over 82 remote branches → committed manifest. **No deletions.**
- Cost: **~8 h** *(unpriced in rev 1)*.

## 4. Deferred — do not work

Legal (all). Credential-lifetime copy. Telemetry. Self-serve export/delete. Rich avatars. Billing.
30-day canary.
**From QM:** shadow deliveries (adds a column + branch to a path about to freeze); tape/replay
determinism harness (revisit at host adapter #3); **CI** (high value, wrong time).
**Reviewer-depth scaling** — deferred: it licenses *shallower* review at exactly the moment the flat
gate is the protection, and it is inconsistent to defer CI on freeze-risk grounds while editing the
review rule mid-freeze.

## 5. QM recommendation A — RETRACTED

The QM analysis recommended building a test-reach checker as the one item worth critical-path time.
**That was wrong.** `tests/p1-cli/test-gate-coverage.test.ts` already does it (D-030), is in the
`npm test` literal list, and was hardened by five printed mutation controls after a rejected-then-
corrected exact review. The QM analysis reasoned from an external repo's solution instead of
enumerating our own suite — the precise "pattern-match instead of enumerate" failure our doctrine
names, and the D-025 incident it cited is the incident that *produced* D-030's fix.
Surviving QM items: durable-by-default (Stage 0), the `resetSession` reachability check (Stage 5),
and the deferrals/rejections in §4.

## 6. Standing verification rules

Measure the artifact, not its name. **Re-measure; do not trust your own earlier measurement.**
Positive control on the same invocation. Enumerate, don't pattern-match. Pushed ≠ landed ≠ applied.
Review the decision set. Corrections go in the artifact. Brace `${rev}:src/...` in zsh. Never name a
zsh loop variable `path`. State what you did **not** establish.

## 7. Estimate and shape

**40–55 h** *(rev 1's 30–40 h omitted Stage 12, review-arm execution, and rework cycles)*.
~12 h strictly serial on the DB slot. Stage 11 hard-blocked on the operator's second machine.
Stage 9 onward operator-gated.

**Check-in: end of Stage 6** — the last fully reversible moment. It carries less information than
end of Stage 7, but it is the last point where changing course is free.

## 7b. Stage 7 coverage truth (updated 2026-08-03 — read before freezing)

`docs/design/2026-08-03-STAGE7-CAUSAL-CONTROL-REGISTER.md` enumerates the ten domains Stage 7
requires. ~~**0 of 10 have a causal control.**~~ ~~**Now 3 of 10; seven remain blind.**~~ The second
statement is **dead (2026-08-03):** a subsequent lane ran seven more exact production mutants under
named per-test watchdogs, closed six domains, and stopped honestly when the claim one-winner mutant
stayed green. **Now 9 of 10 domains have recorded causal controls; claim one-winner remains blind.**

The register contains **22 independently selectable mutant/test pairs**: the prior 15 plus seven
new rows. Enqueue trigger and migration backfill have separate controls. Response-loss replay,
persist-before-ACK, note-with-zero-prompts, stale-lease requeue, and poison ceiling each printed a
named causal red before byte-identical restore. The note control stops at the earlier runtime-stop
assertion rather than the later prompt-count assertion, so it proves that the exact production
mutation is caught but does not isolate the prompt-count observer by itself.

Claim one-winner is still unproven: removing both explicit production lock clauses left the exact
concurrent-claim observer green. The mandatory-stop rule forbade adjusting the mutant until it went
red, so no control credit is claimed. Stage 7 must run all 22 recorded controls and preserve this
one-domain limitation rather than infer coverage from a green observer.

## 8. What this plan does not establish

It does not accept either candidate SHA (1a is code-reviewed but unmerged, with 2.5 of 3 controls
outstanding and no cross-family arm; 1b is unreviewed), prove the integrated union, establish
v0.1.5, or establish production correctness. Estimates are estimates. The Fable review that produced
rev 2 did not verify the content of 1a's code-review claims, the 331/331 and 38/38 counts, any Phase
B code, Grok's actual credit state, or whether `agy`/Gemini currently returns verdicts.
