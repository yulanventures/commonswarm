I'll review lane 2a at `5071a703` against the brief, Fold 1 rulings, and the round-1 failures. No files will be changed.The fold is three commits on top of the round-1 tree. I’ll read the restart command, retry check, and parent-evidence paths against the rulings.The restart sentence, retry wait, and parent classifier are in view. Next I’ll run the lane’s loopback tests and probe each start method the restart command claims to cover.The probe was going to rerun the printed command against a different port than the one in the command. I’ll keep the original loopback server up for that rerun.**PRODUCTION** — `src/cloud/arrival-watch.ts:76-77`. A watcher started with `--agent-token-stdin` prints a restart command that the shell cannot run. The credential instruction is appended to the command, before the period:

```76:77:src/cloud/arrival-watch.ts
  const stdin = options.agentTokenStdin ? " with the same credential input on stdin" : "";
  return `inbox --notify stopped because of ${signal} and nothing is watching this inbox now; restart it under the session's Monitor with ${notifyRestartCommand(options)}${stdin}.`;
```

`src/cli.ts:4764` sets `agentTokenStdin` from that flag. `src/cli.ts:4551-4573` then requires exactly one positional. Taking the command the same way the token-file test does (`with (cswarm inbox --notify .*)\.$` at `tests/p1-cli/resume.test.ts:680`) yields 17 shell words, ending in `the`, `same`, `credential`, `input`, `on`, `stdin`. Run through `/bin/sh` against a loopback read service, with the credential piped on stdin and a temporary `HOME`, that process exits 1 with `too many positional arguments: expected 1, received 8` and makes no request. The agent token is absent from the sentence. The control at `tests/p1-cli/resume.test.ts:637-638` only matches this text. It never spawns it.

The other checks hold. The repo test `the printed restart command starts a watcher against the same loopback read service` runs the printed token-file command, including a path with a space and an apostrophe, and the restarted child reads the loopback service. A path containing double quotes, a `--profile` start (expanded to the credential file, workspace, `--url`, and `--anon-key`), and a default target with `SWARM_CLOUD_URL` / `SWARM_CLOUD_ANON_KEY` set to loopback all restarted and read that service. The default-target sentence omits `--url` and `--anon-key` and contains no agent token. Retry backoff checks at 0, 60000, 120000, and 180000 ms with one inspector in flight; abort during that wait returns `cancelled` in 34 ms. The repo retry test exits 74. Proven stdout wins, parent evidence is used only when stdout is unproved, and `EPERM` is `parent_alive`. Parser fixtures, the timeout citation, and the injected parent adapter are in place. Idle exit 74, the push-mode check, signals 130 and 143, and the silent non-orphan results passed. 54 tests in the lane files passed. Those files are on `npm test` or the `test:p1-cli` glob.

VERDICT: FAIL
