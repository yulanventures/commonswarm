I'll review SHA `9860904b` directly: first the commit range and tree, then the timeout-table code, then measured claims against a loopback origin only.SHA matches. Next I’ll read the table source, mapping, tests, and the round-3 README so I can check the claimed fixes, not just the commit messages.Source is in. Next I’ll enumerate both refs, inspect mapping, and check how the CLI actually talks to the network so a PASS can’t hide a real timeout.I’m checking how the CLI sends HTTP, what the enumerator actually finds on both refs, and whether check/signal-read can miss the preload and talk to a real origin.Isolation may not be complete: `HOME` is replaced, but `XDG_STATE_HOME` and `SWARM_AGENT_STATE_DIR` are copied from the parent. I’ll measure that on loopback, then run the table tests.The 2x gate on an incomplete check now fails the process. I measured that on loopback. The shipped 3 s budget is a separate mapping from HEAD.

**Question 1 (timeout false PASS).** No. `summarize` in `scripts/timeout-table/core.mjs:29` FAILs when `headroom < 2` before `notMeasured`. Loopback: 2200 ms check vs HEAD 3900 ms, `--acknowledge-not-measured` set, process threw `FAIL rows: src/cloud/agent-check-budget.ts:AGENT_CHECK_TIMEOUT_MS`. Same rule on v0.1.71’s 3000 ms (`2221/3000 → FAIL`). Per-request with no log line stays `NOT MEASURED` (silent-client test). Inventories match mapping both ways: HEAD 100 ids, v0.1.71 98 ids. v0.1.71 check is `src/cloud/agent-check.ts:AGENT_CHECK_TIMEOUT_MS` = 3000; HEAD is `src/cloud/agent-check-budget.ts:AGENT_CHECK_TIMEOUT_MS` = 3900.

**Question 2 (credentials / writes).** Copy is 0700/0600, `expires_at` stripped, `credential.json` profile renamed to `profile.json`. Preload rewrite: profile origin hits 0, target got the reads, command/activity/storage 0. Exit 13 / SIGTERM / SIGHUP / SIGINT tests remove the copy and the git worktree. `runTable --ref v0.1.71` left no extra worktree.

**PRODUCTION.** `run.mjs` `environment()` (line 132) copies the parent env. It sets `HOME` and `XDG_CONFIG_HOME` only. `src/cloud/agent-credential.ts:93-98` uses `SWARM_AGENT_STATE_DIR`, then `XDG_STATE_HOME`, then `HOME`. Sequence: parent has `XDG_STATE_HOME`; `openProfileCredential` creates `$XDG_STATE_HOME/cswarm/agent-credentials/`; `cleanupRunResourcesSync` deletes only `tempRoot`. I measured those two empty dirs after a successful run. They are not the token copy. A stored successor for the same lineage would put `expires_at` back and lock that store. Command POST is still blocked by `preload.cjs` / `writes.cjs`. Tests do not set `XDG_STATE_HOME`.

**Question 3 (tests).** 14/14 pass. Line-shift, per-ref missing/stale, incomplete 70 ms vs 100 ms even when acked, `check_timeout` with `measures_guarded_path: false`, relative `?token=` log path, and exit-path helpers all hit the production functions.

**RIGOUR.** `argsOf` defaults `ref: null` → HEAD 3900 ms. Omit `--ref v0.1.71` and a 1600 ms p95 is acked on HEAD and would FAIL the shipped 3000 ms 2x gate. README example uses `--ref v0.1.71`. v0.1.71 hook hard-exit `setTimeout(..., 3_000)` is `local` / `NOT NETWORK` and never takes the 2x gate; `AGENT_CHECK_TIMEOUT_MS` does.

**Not established.** edge-staging latency; 20-run window; shipped `cswarm` binary as `--client`; successor-adoption write with a live store row; SIGKILL leftover (Node does not run `exit`).

VERDICT: PASS — incomplete check with p95 already over half the budget FAILs (acked or not); v0.1.71 3000 ms and HEAD 3900 ms mappings are exact; origin writes blocked; copy cleaned on the tested exit paths.
