### Review of PART T3b: `mapping.json` (Piece 2 of 4)

This review covers the diff chunk for `mapping.json` (piece 2 of 4) across commit `9860904b` on `main`.

---

### 1. Production & Timeout Mapping Analysis

- **Coverage and Semantics**:
  - The chunk covers the tail of the `v0.1.71` inventory (`src/host/*`, `src/listener/*`, `src/onboarding-cli.ts`, and local constants `src/cli.ts:turnBudgetMs`, `src/cli.ts:deliveryHoldBudgetMs`) and the beginning of the `HEAD` inventory (`site/src/lib/*`, `src/cli.ts`, and `src/cloud/*`).
  - Identifiers strictly adhere to the line-shift resilient schema introduced in `dcb041c0` (`file:name` and `file:name#ordinal`, e.g. `src/host/opencode.ts:timeout#2`, `src/listener/hook.ts:timeoutMs#4`).
  - Non-time bounds (`HOOK_RENDER_BUDGET_BYTES`, `TAIL_SERIALIZED_BUDGET_BYTES`, `WAKE_CLAIMS_PER_MINUTE_BUDGET`, `AGENT_CHECK_BODY_BUDGET`) are correctly classified as `"local"` / `"not-run"` with descriptive bounds explanations.
  - Proxy relationships (e.g. `hook-check`, `hook-check-timer`, `hook-hard-exit`, and `check-site` pointing to `proxy_operation: "check"`) are mapped cleanly.
  - In `HEAD`, `src/cloud/agent-check-budget.ts:AGENT_CHECK_TIMEOUT_MS` explicitly declares `"measures_guarded_path": false` to ensure lower-bound failure semantics apply even when the measured path is partial.

### 2. Workspace Safety & Credential Containment

- **Non-Mutating Operations**:
  - Potentially stateful operations (`browser-agent-connect`, `channel-delivery-deadline`, `channel-signal-timeout`) are marked `"class": "not-run"`, explicitly declaring their potential writes (`"delivery lease and receipt state"`, `"device, principal, or agent credential state"`).
  - The only runnable operations declared in this piece are `"safe-read"`:
    - `auth-settings`: reads public auth settings with empty `writes` and `removes`.
    - `check`: operates strictly within the temporary 0700 private copy (`"private-copy check.json cursor/cache only"`) and specifies removal of the copy.

### 3. Test & Verification Integrity

- Operations satisfy the 9860904b requirement that every operation entry must be explicitly named (e.g. `acp-request-site`, `listener-prompt-site`, `channel-delivery-deadline`, `check-site`).
- Citations match the code regions and preserve traceability across both refs.

### 4. Dependencies & Observations

- **[RIGOUR] Ordering of newly enumerated rows in `v0.1.71`**:
  - `src/cli.ts:turnBudgetMs` and `src/cli.ts:deliveryHoldBudgetMs` are appended at the end of `v0.1.71.rows` (after `src/onboarding-cli.ts`), rather than sorted alphabetically under `src/cli.ts`. In standard JSON deserialization, object key ordering is not semantically significant, so this does not impair runtime execution.
- **Unchecked External Dependencies**:
  1. **Piece 1**: Verification of the beginning of `v0.1.71` (`site/*` and early `src/*` rows, including the opening key for `src/host/claude.ts:requestTimeoutMs`).
  2. **Pieces 3 & 4**: Continuation of `HEAD` rows (the completion of `src/cloud/agent-receive.ts:timeout`, `src/host/*`, `src/listener/*`, `src/onboarding-cli.ts`, and verification that `src/cli.ts:turnBudgetMs` and `src/cli.ts:deliveryHoldBudgetMs` are present in `HEAD.rows` as well).
  3. **Enumerator & Runner (`enumerate.mjs`, `run.mjs`)**: Confirmation that ordinal identifiers generated by AST scanning match the ordinal keys defined in this mapping.

---

VERDICT: PASS (Chunk piece 2 of 4 correctly specifies ref inventories, safe-read boundaries, proxy mappings, and non-run classifications)
