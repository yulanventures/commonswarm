I will review N-edge at the exact SHA, stay off production and the box, and start with the lane log, the diff, and the files that own routing and cutover.HEAD matches the review SHA. Next I’ll read the full lane diff and the deploy files that own routing, Caddy, and cutover.The lane is deploy-only. I’ll read the Caddy, runtime, runbook, and tests next so I can attack routing and cutover with the actual lines.I’ll inspect function CORS, JWT flags, env reads, and how the site and CLI call these paths so the attack list is grounded in the real request shapes.Gates and local Caddy validation are next. I’ll also inspect the adapted JSON so path matchers and the catch-all are not just assumed.Caddy validation failed on a nested read-only mount. I’ll put the dummy certs in the fixture tree and run validate again.Gates passed. I’ll inspect the adapted Caddy JSON for matcher order, timeouts, and the catch-all, then try a loopback runtime check if local Supabase is up.Checked SHA `81159423351907bc33bd209c11cce30fee4c39fe`. Did not call production, the box, Cloudflare, or DNS. Started no other model.

Gates (real exit codes): `npm run build` 0; `npm test` 0; `npm run test:p1-cli` 0 (735 pass); `npm run check:tests` 0; `docker compose -f deploy/edge-runtime/compose.yaml config -q` 0. `caddy:2.11` validate/adapt for both fixtures 0. Loopback runtime against `127.0.0.1:54321`: Kong bare-path 404, unknown 404, preflight headers, command body, H0 GET/HEAD, query string. Removed every container I started.

The function path, Host rewrite, CF header strip, and Realtime `/realtime/v1/websocket` matcher hold. The cutover abort path does not.

## PRODUCTION

**1. After 6b the public API is down, and §8 cannot abort that window.**

`deploy/edge-runtime/RUNBOOK.md:166-175` then `205-208`.

Sequence:
1. Operator runs 6b: deactivates the Supabase custom domain.
2. Clients still use `https://api.commonswarm.com`. DNS still points at Supabase. Supabase no longer serves that host.
3. Site, CLI, and listener calls fail.
4. 6d (GitHub sign-in through the project URL) fails. 6d says do not change anything else until it succeeds. DNS has not moved.
5. Operator opens §8. First step restores DNS that did not change. Next step keeps the new GitHub/GoTrue URLs and asks to verify the same project-URL sign-in that just failed.

User sees: sign-in fails; workspaces do not load; `cswarm` calls to `api.commonswarm.com` fail. The runbook gives no step that reactivates the custom domain and restores the old OAuth URLs immediately.

**2. Browser cannot read the new 504 body.**

`deploy/edge-runtime/main/index.ts:53-60` and `106-111`. `deploy/edge-runtime/compose.yaml:23-24` (`--request-wait-timeout` 10000).

Sequence: four workers stay busy for more than 10 s (DB stall across the ocean, or four slow bodies). A fifth `POST https://api.commonswarm.com/functions/v1/command` from `https://commonswarm.com` (site `site/src/lib/commonswarm.ts:530`). Main returns `504 {"code":"WORKER_LIMIT",...}` with no `Access-Control-Allow-Origin`. Preflight already passed with `*`.

User sees a browser network/CORS failure, not the 504 JSON. The command does not complete.

## RIGOUR

**3. Bare `/realtime/v1` is not the HTTP/1.1 websocket route.**

`deploy/edge-runtime/commonswarm.caddy:81-87` vs catch-all `85-88`. Loopback matcher: `GET /realtime/v1` → catch-all (HTTP/2). `GET /realtime/v1/websocket` → realtime (HTTP/1.1, `flush_interval -1`). Clients use `/realtime/v1/websocket`. Do not reuse the old “six sequential requests” claim; this is a different path.

**4. Boot only checks that `SWARM_SELF_SERVE` is non-empty.**

`deploy/edge-runtime/main/index.ts:37-48`. Command still needs `=== "1"` (`supabase/functions/command/index.ts:728`). `SWARM_SELF_SERVE=0` starts the box and then refuses signup.

**5. Visitor IP for capability depends on a paste outside the site file.**

`deploy/edge-runtime/caddy-global-servers.caddy:1-9`, `deploy/edge-runtime/RUNBOOK.md:70-76`. If that paste is missing, Caddy is still valid. `{http.request.client_ip}` is the Cloudflare peer. Capability keys the rate limit on the rightmost `X-Forwarded-For` (`supabase/functions/capability/index.ts:607-610`). Users at one PoP share one bucket.

Not established: GitHub OAuth through the proxy; 25 MiB upload through Caddy; JWT `iss` after custom-domain removal; a forced live 504; whether the box already has the trusted-proxy paste.

VERDICT: FAIL — step 6b takes api.commonswarm.com down before DNS moves, and rollback §8 has no abort for a failed 6d; it keeps the new OAuth URLs and re-asks the GitHub check that already failed.
