I'll review the lane at the exact SHA: read the commit range, the diff, and the edge-runtime files, then run the named gates. No production, no swarm, no other models.Worktree is at the exact SHA. Next I’ll read the deploy files, the full diff, and the existing edge-function config.I have the lane files. Next I’ll inspect function config, Kong path handling, and previous FAIL notes, then run the named gates.I’ll inspect function CORS, path handling, capability IP keys, and H0 imports, and start the named gates.Docker is local. I’ll extract the runtime error names, run Caddy validation, start the named gates, and probe staging.Build passed. Local Kong is up, so I’ll compare path parity there, run the remaining gates, and probe staging.Cleaning leftover probe state, then I’ll write the findings.SHA `70d17d08`. Gates: `build` 0, `npm test` 0, `test:p1-cli` 0, `check:tests` 0, `compose config -q` 0. Caddy 2.11 adapted both import shapes. Staging GETs matched the claimed routes. Did not touch production, DNS, or Cloudflare.

## PRODUCTION

**1. Full worker pool does not return 504 WORKER_LIMIT**

`deploy/edge-runtime/main/router.ts:43-46`, `140-142`; `deploy/edge-runtime/main/index.ts:91-98`; `deploy/edge-runtime/compose.yaml:23-24`; claim at `deploy/edge-runtime/README.md:17-19`.

The wait-timeout error on the pinned image is `InvalidWorkerCreation`, not `WorkerRequestCancelled` / `WorkerRequestIdleTimeout`. I ran `edge-runtime:v1.73.13` on loopback, `--max-parallelism 1 --request-wait-timeout 1000`, five concurrent GETs to a 2.5 s worker. Four answers:

`599 {"ok":false,"name":"InvalidWorkerCreation","message":"InvalidWorkerCreation: worker did not respond in time"}`

The box catch only maps those two other names to 504. This name falls through to 500.

**Request:** occupy all four workers for more than 10 s (hung `POST /functions/v1/read` or `command`), then a fifth `POST /functions/v1/command`.

**User sees:** after 10 s, HTTP 500 `{"error":"internal_error"}` with `Access-Control-Allow-Origin: *`. Not 504 `{"code":"WORKER_LIMIT",...}`. The suite never throws `InvalidWorkerCreation`. `VERIFICATION.md:148` says the exhausted-pool path was not forced.

Normal 3 s traffic with four slots and a 10 s wait still fits. This is the hang/incident path, and the mapping they added is wrong.

## RIGOUR

**2. Retired-worker retry does not wrap `fetch`**

`deploy/edge-runtime/main/index.ts:74-88` vs upstream `callWorker` (retries `WorkerAlreadyRetired` around create+fetch). Sequential reuse on the pinned image did not fail for me. An in-flight fetch that dies at wall-clock still becomes 500 `internal_error`.

**3. Caddy 502 on runtime restart has no CORS**

`deploy/edge-runtime/commonswarm.caddy:53-61` has no `handle_errors`. While `edge-runtime` restarts, a browser `POST /functions/v1/command` from `https://commonswarm.com` sees a CORS failure, not the 502 body.

**4. Docker kill is 10 s; graceful-exit is 70 s**

`deploy/edge-runtime/compose.yaml:29-30` and no `stop_grace_period`. `docker compose restart` SIGKILLs at 10 s. An in-flight command is cut. The reader sees a dropped connection.

**5. Local Kong OPTIONS is not the 200 they recorded**

This stack’s Kong 2.8.1: `OPTIONS /functions/v1/command` → **204** from the worker (`POST, OPTIONS`, `authorization, content-type, apikey`), `X-Kong-Upstream-Latency: 21`. `VERIFICATION.md:140-142` says local Kong returned 200 and the long method list. Staging returns that 200 list. Site `fetch` headers still pass. The local control is wrong; hosted plugin parity is the one they shipped.

**Not a new fail:** `/functions/v1` vs `/functions/v1/` matches Kong (JSON no-route vs `Function not found`). Runbook order matches the 21:07Z ruling, with rollback before and after the custom-domain step. `SWARM_SELF_SERVE` must be `1`. Realtime matcher includes `/realtime/v1` and `/realtime/v1/`. Crash-loop and six-request starvation stay refuted.

**Not established:** hosted production OPTIONS (no production contact); a 4-wide hang on the live box; `cswarm check` at 3 s vs box feed p95 2.93 s (N-db / DNS rollback, not this mapping).

VERDICT: FAIL — pool wait on v1.73.13 throws InvalidWorkerCreation and the main service turns that into 500 internal_error, not the claimed 504 WORKER_LIMIT.
