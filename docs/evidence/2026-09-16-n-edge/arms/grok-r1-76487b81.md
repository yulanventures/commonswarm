I'll review the N-edge lane at the exact SHA: read the commit range, then attack routing, env, timeouts, and the runbook against the binding.The range is three commits and twelve new files. Next I’ll read the router, Caddyfile, compose, runbook, and the function env usage.Next I’ll inspect how the five functions parse paths and env, then check whether the runbook matches the binding cutover.Local Supabase is up and the images are already here. I’ll start the gates and probe Caddy plus edge-runtime on loopback.SHA `76487b81da35a9afed573283113d65cbc5c277e5` (detached). Range is three commits on top of `fca81374`. I did not contact production, the box, Cloudflare, or DNS. I started no other model.

**Gates (real exit codes)**

| Gate | Exit |
|---|---|
| `npm run build` | 0 |
| `env -u FORCE_COLOR npm test` | 0 |
| `env -u FORCE_COLOR npm run test:p1-cli` | 0 |
| `npm run check:tests` | 0 |
| `docker compose -f deploy/edge-runtime/compose.yaml config -q` | 0 |
| `caddy:2.11 caddy validate` with dummy certs | 0 |

Local loopback probes on `127.0.0.1:19000` against the pinned image and the local stack at `127.0.0.1:54321`: path strip, query, trailing slash, unknown name, OPTIONS, body, `Authorization`, 413, missing env, test hooks, and `parity-smoke.mjs` all ran. Containers I started are gone.

---

## PRODUCTION

**1. The runbook does not follow the binding cutover, and rollback cannot reverse it.**

`deploy/edge-runtime/RUNBOOK.md:114-142` and `:156-167`.

Binding: rehearse Host rewrite on `edge-staging.commonswarm.com`; in the window, **deactivate** the Supabase custom domain, **move** the GitHub OAuth callback and GoTrue URLs to the project URL, **verify one GitHub sign-in**, keep 48 h fallback.

What the file actually does:

- Step 5 never names `edge-staging.commonswarm.com`. It sends `api.commonswarm.com` at the box with `curl --resolve` while the custom domain is still live (`RUNBOOK.md:122-130`).
- Step 6 keeps that custom domain for 48 h (`RUNBOOK.md:138-140`). It never deactivates it. It never moves GitHub or GoTrue URLs. It never asks for one GitHub sign-in after those moves.
- Rollback only restores the saved DNS row, then stops Compose (`RUNBOOK.md:158-167`). It does not put the custom domain or OAuth URLs back.

The Caddyfile already lists the staging name (`commonswarm.caddy:13`). The runbook never uses it.

**Request:** GitHub sign-in after DNS moves, with Host rewritten to `ukezjcnxjvkpkeezxaew.supabase.co` (`commonswarm.caddy:2-6`) while GitHub/GoTrue still name `api.commonswarm.com`.

**User sees:** GitHub `redirect_uri` mismatch, or a callback that never returns to the site. Sign-in is broken. Rollback that only flips DNS cannot undo URL moves that were never written down, and cannot undo a custom-domain deactivation the runbook never did.

**2. Caddy rewrites `Host` but still sends `X-Forwarded-Host` as the public name.**

`deploy/edge-runtime/commonswarm.caddy:1-8` and `:26-29`. Caddy 2.11 sets `X-Forwarded-Host` to the client Host by default.

**Request:** `GET https://api.commonswarm.com/auth/v1/authorize?provider=github`

Upstream gets `Host: ukezjcnxjvkpkeezxaew.supabase.co` and `X-Forwarded-Host: api.commonswarm.com`. I did not call GoTrue (that would hit `*.supabase.co`). If GoTrue builds `redirect_uri` from `X-Forwarded-Host`, GitHub and GoTrue disagree after the binding’s URL move. Same user-visible sign-in failure as (1).

---

## RIGOUR

**3. Gateway CORS is gone. The function CORS is stricter than Kong.**

Local Kong `OPTIONS /functions/v1/command` → **200**, `Access-Control-Allow-Origin: *`, `Allow-Headers` echoes `x-client-info`.

This runtime → **204** from `command/cors.ts:49-61`. Allow-headers are fixed: `authorization, content-type, apikey`. Evil origin gets no `Allow-Origin`.

First-party site `fetch` uses only those three headers, so it still works. A browser that sends `x-client-info` (supabase-js `functions.invoke`) fails preflight. The user sees a CORS error in the console, not a JSON body.

Unknown name: Kong `404` text `Function not found` with `Allow-Origin: *`. This runtime `404 {"error":"function_not_found"}` with no CORS (`main/index.ts:79-80`).

**4. Time-to-first-byte is 65 s, not hosted 150 s. Failures become 500.**

`compose.yaml:21-22`, `commonswarm.caddy:21`, `main/index.ts:29` and `:99-103`.

`--user-worker-request-idle-timeout 65000` is time from worker takeover until a response (hosted idle timeout is 150 s, then **504**). Caddy `response_header_timeout 65s` matches. The main `catch` turns `WorkerRequestIdleTimeout` into **500** `{"error":"internal_error"}`.

CLI and site abort at 30 s today, so current callers die first. A later 50 s poll has 15 s of margin. A cold worker plus ocean DB can eat that. The user sees 500, not 504.

`--max-parallelism 6` plus `--request-wait-timeout 10000`: a 7th overlapping call waits 10 s, then the same 500. Six 50 s polls fill the pool. `userWorkers.create` does not retry `WorkerAlreadyRetired` (the upstream example does). No `cpuTimeSoftLimitMs` (hosted CPU cap is 2 s).

**5. Path extras Kong would also see, but tests do not pin them.**

Loopback: `/functions/v1/h0/agent-doc/parity` → 200; `?x=1` → 200; trailing slash → 404 `not_found` (`h0/core.ts:251`). `/functions/v1/command` and `/command/` and `?apikey=` all 400 for `{}`. Body is forwarded: `{"command_id":"123e4567-e89b-12d3-a456-426614174000"}` → 401, not 400. `Authorization` is forwarded (parity read HTTP 200). 413 on a real oversized body.

Caddy `handle /functions/v1/*` does not match `/functions/v1` with no extra segment. That path hits the catch-all (`commonswarm.caddy:47-50`) and goes to hosted Kong. The router tests never cover query, slash, OPTIONS, or headers (`tests/p1-cli/edge-runtime-box.test.ts:41-63`).

**6. No `trusted_proxies`.**

Caddy is meant to sit behind Cloudflare. Without that option, `X-Forwarded-For` is the Cloudflare edge, not the client. Capability keys its 120-read bucket on the rightmost XFF (`capability/index.ts:607-610`). If `SWARM_CAPABILITY_URLS=1`, every caller shares one bucket. User sees 429 `rate_limited`. Production secrets named today do not include that flag, so this is dark unless someone sets it.

**7. Env boot and test hooks are sound.**

Missing required names: process exits 1, log `edge-runtime environment is missing: SUPABASE_URL, …`. Test hooks in the container env with `SWARM_ENV=development`: command still 400, not the boot throw. Filter in `main/index.ts:59-69` holds. `SWARM_SELF_SERVE` is not in `REQUIRED_ENV`. If the box file omits it, `/app` create-workspace stays 403.

---

## What I did not establish

Live `/auth`, `/rest`, `/storage`, or `/realtime` through this Caddyfile (that is `*.supabase.co`). Whether the Origin CA cert includes `edge-staging.commonswarm.com`. Whether hosted GoTrue reads `X-Forwarded-Host`. Production concurrency vs 6 workers. I did not start Caddy as a listening proxy.

Loopback routing for the five functions, Host+SNI rewrite in the adapted Caddy JSON, loopback bind `127.0.0.1:9000`, and image listen `0.0.0.0:9000` were measured.

VERDICT: FAIL The runbook skips the binding (staging rehearsal, custom-domain off, GitHub/GoTrue URL move, one GitHub sign-in) and cannot roll those steps back, so cutover sign-in is unsafe.
