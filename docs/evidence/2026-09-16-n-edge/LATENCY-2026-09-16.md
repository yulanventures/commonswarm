# N-edge latency, measured 2026-09-16 ~20:05Z

From the operator's mac mini, through the real `cswarm` CLI, 15 alternating runs per path and operation
(`latency-harness.py`). End-to-end wall time; CLI startup measured separately at 0.06 s p50. All 90 runs exited 0.

- production: `https://api.commonswarm.com` (Supabase edge functions, us-east-1 database)
- box: `https://edge-staging.commonswarm.com` → Cloudflare (proxied) → yulan-vps-1 (Falkenstein) → edge-runtime
  container at lane SHA 76487b81 → production database via role `commonswarm_edge` on the aws-0-us-east-1
  transaction pooler (TCP connect 108-113 ms from the box).
- The mini's system resolver still held a negative answer for the new staging name, so the harness routes only that
  hostname to a Cloudflare IP with a Node `--require` preload (`latency-dns-override.cjs`); no system setting changed.

| operation | edge | production p50 / p95 | box p50 / p95 |
|---|---|---|---|
| `members` | read | 0.845 / 1.163 s | 2.774 / 3.317 s |
| `feed --limit 5` | read | 0.733 / 1.087 s | 2.072 / 2.933 s |
| `receipt <signal>` | command | 0.696 / 0.900 s | 1.683 / 2.107 s |

Estimated database round trips from the gap at ~110 ms: read ≈ 17 per request, receipt command ≈ 9.
`pg_stat_statements` cannot isolate them: after `SET LOCAL ROLE` statements are attributed to swarm_read /
swarm_command, which production also uses; only 4 wrapper statements (98 calls) appear under commonswarm_edge.

**HezLead's cap (60949772): command p95 > 2.0 s → stop and tell HezLead.** Tripped at 2.107 s. Reported
(0471b983), cutover stopped pending the ruling.

## Realtime through the box (~20:15Z, `realtime-probe.mjs`)

supabase-js 2.110.8, legacy anon key, a random broadcast channel with `self: true`, one message:
| path | subscribe | broadcast round trip |
|---|---|---|
| production api.commonswarm.com | SUBSCRIBED 421 ms | 620 ms |
| box edge-staging (Caddy → supabase.co with Host rewrite, at 76487b81 without `versions 1.1`) | SUBSCRIBED 1354 ms | 1516 ms |
The websocket upgrade works through the box's proxy as deployed; the box operator still requires `versions 1.1` on
that upstream (N-edge fix round 1).

**Strategist ruling 23a9116a: B — proceed on Falkenstein** with this table in the release note and the resume file,
the Supabase URL as the 48 h fallback, and any production client timeout after cutover meaning a DNS rollback.

NOT MEASURED: a seat's wake round trip; H0's poll re-check (not built); latency from any location other than the mini.
