I'll review only the diff from `c32abeb5` to `0a45e791` and check the rate-limit sentence, the profile next-step, and whether anything else moved.The diff is three files. I'll check the edge rate-limit path, the profile next-step wording, and whether F4-3 matches both.No PRODUCTION or RIGOUR defects. The range is exactly `b52245b3` and `0a45e791` on `c32abeb5`, and the diff is only `src/mcp/errors.ts`, `tests/p1-cli/mcp-stdio.test.ts`, and the F4-3 cell in `docs/evidence/2026-09-23-mcp-lane2/LANE.md`.

**Rate limit.** Both buckets return the same HTTP 429 with `error: "rate_limited"`. `SIGNAL_CREDENTIAL_LIMIT` is 120 and `SIGNAL_WORKSPACE_LIMIT` is 1000 (`supabase/functions/command/index.ts:652-653`). `enforceSignalRate` counts the credential bucket first (`signal:credential:…`, agent key `auth.credentialId`, the token id at `:2790`) and returns immediately when `count > 120` (`:5238-5248`). Only if that bucket is still inside the limit does it count `signal:workspace:${workspaceId}` and return when `count > 1000` (`:5250-5260`). The post path turns either result into one body: status 429, `error: "rate_limited"`, plus `limit` and `resets_at` (`:9704-9744`). Both windows are `date_trunc('hour', …)` and `resets_at` is `window_start + 1 hour` (`:5101-5110`), so each refusal resets within an hour. `ThinCommandClient.sendSignal` keeps the `error` slug and does not read `resets_at` (`src/cloud/command-client.ts:1412-1425`, class at `:431-439`). MCP then replaces the producer text with the owned sentence (`src/mcp/errors.ts:65`, `:146-151`). The new sentence is true for both refusals. The old sentence, "for this agent was reached," was false for a workspace refusal, because that response is returned only when the credential count is not over 120.

**Profile next step.** `--profile` is a process flag on `cswarm mcp` (`src/cli.ts:9377-9381`). None of the seven tool schemas has a profile argument (`src/mcp/tools.ts:28-36`). `profile_path_invalid` is a non-absolute path or a path with control characters (`src/cloud/agent-profile.ts:47-51`). `profile_symlink` is an ancestor symlink other than `/tmp` or `/var` (`:62-66`). `profile_inside_repository` is an ancestor `.git` (`:70-72`). The model cannot change that path from a tool call, so "a person must restore this agent's access outside this session" (`src/mcp/errors.ts:14`, `:24`, `:30-31`) is the right next step, the same step the other `profile_*` rows already use. The new test (`tests/p1-cli/mcp-stdio.test.ts:154-158`) takes every `MCP_ERROR_SENTENCES` key that starts with `profile_` and asserts the next step is not `"fix the named argument"`. `mapMcpError` copies that field (`src/mcp/errors.ts:146-151`), so putting that step back on any of those codes fails the assertion. There are 9 such keys today, including the three location codes. The test passed: 1/1.

**F4-3.** The correction matches the edge: one 429 `rate_limited` for the 120-per-hour credential bucket and the 1000-per-hour workspace bucket, the old agent-only sentence was false for the workspace bucket, the next step is still wait and retry with the same `request_id`, and the command client still does not type or parse `resets_at`.

VERDICT: PASS
