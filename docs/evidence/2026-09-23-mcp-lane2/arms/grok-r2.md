I'll review lane 2 at `8321eae3` as a read-only second arm: the fold record, the brief, and the diffs, then check each round-1 finding against the code and the tests that are supposed to fail if the fix is reverted.The fold is five commits on `064a1cdd`. I'll read the lane record, the brief, and the MCP sources those commits changed.The fold record and the three MCP modules are in view. Next I’ll compare them with the tests and the signal, check, and CLI changes they depend on.The fold fixes the round-1 wire bugs it names: owned sentences replace producer prose, a committed post that comes back malformed or as HTTP 5xx is `unknown`, cancellation writes no JSON-RPC result, and a deferred cursor commit re-reads `check.json` and moves only forward. Those four are locked by tests that assert the owned sentence, the unknown object, the absent cancelled id, and the later cursor. The lane still fails on the recovery text those errors tell the model to follow.

## PRODUCTION

**Reachable command refusals tell the model a person must restore access.** `src/mcp/server.ts:121` rethrows every `CommandHttpError` in 400–499 that carries a code, so these are definitive tool errors. `src/mcp/errors.ts:113-114` then assigns one next step to every code missing from the table: HTTP status under 500 becomes "a person must restore this agent's access outside this session". `tests/p1-cli/mcp-stdio.test.ts:365-368` locks that choice for a made-up 418.

That step is false for refusals this server actually returns, and the signal is not created:

- A reply the caller may not answer, or a `to` that is not live, is HTTP 403 `forbidden` (`supabase/functions/command/index.ts:9666-9676`, `src/cloud` target resolution returns null at `supabase/functions/command/index.ts:8481` and `8504`). The table sentence is "The service refused this agent's access" (`src/mcp/errors.ts:52`). The CLI already treats that same 403 as "you are not the addressee" (`src/cli.ts:4020-4032`). The model is told to stop and have a person restore access.
- Signal rate limit is HTTP 429 `rate_limited`, 120 signals per credential per hour (`supabase/functions/command/index.ts:652`, `9736-9743`). The next step says to restore access. The same request id can succeed after the window.
- An unknown or archived channel is HTTP 404 `channel_not_found` or HTTP 409 `channel_archived` (`supabase/functions/command/index.ts:8131-8149`). The producer text names the slug and the live channels. The mapper drops that text, echoes the slug, and says a person must restore access. The model can change `channel`.

`ask`, `note`, `reply`, and `working_on` all hit this path. A wrong reply id, a typo in a channel, or the hourly cap stops the session with the wrong instruction.

## RIGOUR

**The byte-cap test does not lock a shortened page.** `capFreshCheck` keeps a prefix and returns that prefix's last id (`src/mcp/tools.ts:95-102`). The commit stores that id only when it is still ahead of the cursor on disk (`src/cloud/agent-check.ts:228-236`), and the handler registers the commit only when `lastVisibleId` is set (`src/mcp/server.ts:91-94`). A page the model actually received is not skipped, and it is not returned again. The stdio test forces the cap with a workspace name of `MCP_RESULT_MAX_BYTES` (`tests/p1-cli/mcp-stdio.test.ts:460-467`). That name makes even an empty projection overflow, so `lastVisibleId` stays null and no commit is registered. The fake honors `after_id`, so this proves the cursor stays put when nothing was shown. A mutation that still commits the full page whenever a non-empty prefix is shown would pass.

**Startup stderr still carries a flag and a shell command. One JSON-RPC error echoes the caller's key.** `mapMcpError` does not copy `error.message`, a path, or a shell command. Unlisted codes are echoed only when they match `^[A-Za-z][A-Za-z0-9_.-]{0,63}$` and do not contain `--` (`src/mcp/errors.ts:110-114`). Teammate bodies in `check` are the message text, on purpose. Two other strings are outside that mapper:

- Process startup prints `AgentSetupError.message` (`src/cli.ts:9742`). Managed startup says "needs `--host-session-id`" (`src/mcp/server.ts:37`). A missing profile says "Run cswarm setup with the connection file" (`src/cloud/agent-profile.ts:143`). The owned sentences for those codes are not used on this path.
- An unknown tool key is interpolated into the JSON-RPC error and is not passed through the byte cap: `Unknown argument: ${key}` (`src/mcp/tools.ts:45`, `src/mcp/server.ts:69`).

**The new recipient bound changes one CLI case. The older four sentences are the same text.** `resolveSignalRecipient` now throws `SignalRecipientError` `recipient_invalid` with "signal recipient name is too long or empty" when the selector is empty or longer than 80 (`src/cloud/signals.ts:1481`, `src/cloud/signal-limits.ts:5`). The ambiguous-id, unknown-id, ambiguous-name, and unknown-name sentences are unchanged, and the CLI prints `error.message`. `swarm.users.display_name` is unbounded `text` (`supabase/migrations/20260723000001_p1_schema.sql:113`), so a unique display name longer than 80 characters no longer resolves on `cswarm ask` or `cswarm note`. The MCP schema rejects that length before the resolver. No CLI test covers the new sentence.

**The generated dispatch rows for `mcp` still die on the extra positional.** `policy.host-session.mcp.keep` and every `selected-error.mcp.*` row are built from `["mcp", "extra"]` (`tests/p1-cli/command-dispatch-baseline.test.ts:506`). Their recorded stderr is `too many positional arguments`, and `handlers` is empty. The three added fixtures do reach startup checks: missing `--profile` stops in `assertShape`, a missing file reaches `profile_missing` inside `serveMcp`, and `--host-session-id manual` reaches `host_session_invalid`. The fold's "reach MCP policy" sentence (`docs/evidence/2026-09-23-mcp-lane2/LANE.md:64`) does not describe the policy-matrix rows. The prior 1,258 baseline objects are unchanged and the file now has 1,261 rows; that count claim is true.

**`LANE.md` row 6 still states the retired cancellation behavior.** It says a cancellation after send start puts `unknown` on the stdio wire (`docs/evidence/2026-09-23-mcp-lane2/LANE.md:15`). Fold 1 says that section supersedes it, and the code matches the correction: the SDK drops a handler return once the request is aborted (`node_modules/@modelcontextprotocol/sdk/dist/esm/shared/protocol.js:368-372`), and the explicit `transport.send` is gone. The brief's dated corrections match the code: `replayed` is gone, a cancelled id gets no result, and other failures after `sendSignal` begins are `unknown` unless a typed 4xx carries a code. Pre-send recipient, duration, and credential failures are outside that `try` (`src/mcp/server.ts:103-112`) and stay tool errors.

`dist/` is not built here, so the recorded 17/17 gate was not re-run.

VERDICT: FAIL
