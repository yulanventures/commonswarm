You are the Maker for fold 4 on item H lane 2, "cswarm mcp over stdio, release 1". Work ONLY in your current
directory, the git worktree of branch lane/mcp-stdio (tip d7c17124: your folds 1-2 plus a lead fix). Read LANE.md,
then the round-3 reviews: /private/tmp/claude-501/-Users-yulanbot-Developer-Ridge-io-cloud-swarm/348bdb35-59a1-416f-9db8-84f388f5c3e0/scratchpad/mcp/grok-r3.md (FAIL; its table of edge codes for post_signal is the reference) and
/private/tmp/claude-501/-Users-yulanbot-Developer-Ridge-io-cloud-swarm/348bdb35-59a1-416f-9db8-84f388f5c3e0/scratchpad/mcp/opus-r3.md (PASS with notes N1-N4).

HARD RULES: as for the lane — no production host (api.commonswarm.com, commonswarm.com,
edge-staging.commonswarm.com, 178.105.29.28, 100.115.66.74), no Supabase or Vercel service, no swarm/cswarm against a
real workspace, do not touch ~/.cswarm or ~/.config/cswarm, no other model, no skill, no auto-approve flags, no secret
printed, no push or rebase, no other worktree. Real exit codes; never pipe a gate into grep. Every test has a timeout;
no process of yours survives. D-053: never branch on error.message.
SANDBOX: network access and the repository .git directory are writable: commit yourself and run every gate.

LEAD RULINGS (each with a test that fails when the fix is reverted; record mutations in LANE.md "Fold 4"):
F4-1 (Grok 1, PRODUCTION). The post-path `forbidden` sentence states causes, never permissions. Use exactly:
  "The service refused this post. Possible causes: a reply to your own ask; a reply to a signal that has expired or
  is not addressed to this agent; a recipient that is no longer active; a change to this agent's access."
  Keep the next step "check the named arguments; if they are right, a person may need to restore this agent's access"
  (use the existing constant). Update the test that pins the string.
F4-2 (Grok 2, Opus N2). Add owned sentences for the managed-session codes the edge returns to post_signal and to the
  read path: session_proof_missing, session_proof_invalid, session_expired (401) and session_conflict (409). Their
  next step: "restart this MCP server with the current host session" (new constant, no flag or command name).
  Derive the code list from the exported session-wire constants (src/cloud/session-wire.ts), and extend the generated
  coverage test so a missing session code fails it.
F4-3 (Opus N1). rate_limited: sentence "The signal rate limit for this agent was reached; it resets within an hour."
  next step "wait, then retry the same call with the same request_id" (new constant). If the edge body carries a
  reset time the client already parses, include it as `retry_after` (ISO time) in the payload; do not add parsing
  for a field the client does not already type.
F4-4 (Grok 3). Channel enforcement equals the advertised schema: advertise the reserved slugs as
  `not: { enum: [...] }` generated from the same constant channelSlugProblem reads (export it if needed), and have
  validateMcpArguments enforce exactly schema pattern + maxLength + not-enum (no second classifier). Test: the
  advertised schema rejects `all-signals` and the server returns -32602 for it.
F4-5 (Grok 4). The partial-page test must fail when the byte cap stops dropping rows: build the fixture so the check
  library returns ALL its messages on the first page and only capFreshCheck shortens it; mutation: make capFreshCheck
  return every message -> the test fails. Keep the existing no-skip/no-repeat assertions.
F4-6 (Grok 5, Opus N4). LANE.md: correct Fold 1 R9 (no channel normalisation; the 80-character recipient bound is
  MCP-schema only; the stdio test rejects TEAM-UPDATES and posts team-updates unchanged) and put an inline
  "(superseded by Fold 1: ...)" marker on row 6's old cancellation wording, keeping the retired words quoted. The R10
  policy-row claim: add the three fixture ids to the focused assertion's comment only; no new test required.

COMMIT(S): "fix(mcp): ..." / "test(mcp): ...", trailers exactly:
Agent-Name: Yulan Bot
Agent-Model: <your model id>
Agent-Family: openai
Agent-Tool: codex <version>
Agent-Model-Source: runtime-ambiguous

GATES (each with its exit code and count): npm run build ; env -u FORCE_COLOR npm test ;
env -u FORCE_COLOR npm run test:p1-cli ; npm run check:tests ; bash scripts/build-release.sh (then run
dist-release/cswarm mcp --help from a temporary directory outside the repo) ; git diff --check origin/main...HEAD.
(Tests that call ps may fail with EPERM in your sandbox; name them; the lead reruns the gates outside it.)

REPORT: commit SHAs; each ruling -> implementation -> test -> mutation result; gate exit codes with counts; what you
did not establish.
