I'll review the round-6 fixes on `a1a3fd74` without changing anything: the diff from `ec44cc95`, whether the index itself stayed the same, and the three checks on the read-edge probe, catalog proof, and new claims.The diff is the catalog proof, the catalog test, and LANE.md plus saved probe output. Next I’ll check the read-edge body against the validator and whether the index definition itself moved.The migration file matches `ec44cc95`. I’ll check the saved plans against the timing sentences, then measure the index clause and the read-edge body on the local stack.The installed index text matches the proof on Postgres 17.6, and a well-formed probe body is accepted (401 for an unknown token). Next I’ll run the wrong-index cases in a transaction and roll them back.No findings. The migration index is unchanged, and the three round-6 fixes hold on the local stack.

The index definition is the same object reviewed in round 6. `supabase/migrations/20260925000001_unclaimed_observed_ack.sql` is byte-identical at `ec44cc95` and `a1a3fd74` (sha256 `513f3512…ddb8107`). This diff does not touch that `CREATE INDEX`. It changes the catalog proof, the catalog test, `LANE.md`, and saved probe output.

**Read-edge probe.** The body in `docs/evidence/2026-09-25-item-g-lane1/LANE.md:16` is the agent signals shape `parseBody` accepts: `in_reply_to` present, both cursor keys present, no `channel`. `supabase/functions/read/index.ts:298-311` requires exactly those keys, and lines 327-333 accept both cursors null. On `127.0.0.1:54321`, that body with a UUID workspace and a well-formed unknown `swm_agt_…` token returned **401** `unauthenticated` (past the validator). The same token with `workspace_id` removed returned **400**; an unexpected extra key returned **400**. The steps run after the note is posted. Both cursor keys null set `cursor_mode`, so `index.ts:820-821` pages oldest first with no lower bound (`824-827`) and `LIMIT` 50 (`933`). Under fewer than 50 live directed rows, the note’s `id` is in `signals`. A fresh seat has the acknowledged signal plus the unchecked note (defaults keep both live: ask 7 days, note 30 days at `command/index.ts:647-651`). The signals path only selects (`856-934`); it does not acknowledge.

**Index clause.** On local Postgres 17.6 the installed definition equals the proof string, and the full catalog proof returns `t`:

```77:77:deploy/release-proofs/item-g/20260925000001-catalog.sql
    SELECT pg_get_indexdef(i.oid) = 'CREATE INDEX signal_deliveries_unclaimed_observed ON swarm.signal_deliveries USING btree (workspace_id, recipient_agent_principal_id) WHERE ((ack_outcome = ''observed''::text) AND (last_lease_id IS NULL) AND (last_leased_by IS NULL))'
```

In one rolled-back transaction, after the same view replace the test performs: dropping the index, adding `AND acked_at IS NULL`, and creating the unique index each make the index clause `f` and `catalog_ok` `f`, while every other conjunct stays `t`. The false result is that clause. Rollback restored the original index (`restored_eq` `t`).

**New `LANE.md` claims.** Saved plans match the rewritten row at `LANE.md:158`: `lead-base.out` 1599.382 ms with no use of this index; `lead-idx.out` 280.099 ms with both `observed` and `later` on `signal_deliveries_unclaimed_observed`; `lead-migrated-1.out` 61.113 ms (heal via a bitmap index scan), `lead-migrated-2.out` 49.992 ms and `lead-migrated-3.out` 51.138 ms (heal seq scans), known-seat index scan in all three; `opus-r6-migrated-1.out` 262.275 ms and `opus-r6-migrated-2.out` 281.674 ms; `build-cost.out` builds the partial index on the 1,000,000-row copy in 39.030 ms at 8192 bytes. Each output ends `leftover: 0`. The new evidence files contain no credential, token, or connection string.

VERDICT: PASS
