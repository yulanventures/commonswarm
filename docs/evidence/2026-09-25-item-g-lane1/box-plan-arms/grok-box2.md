I'll review the lane as a read-only pass: first the round-1 findings, then the fixes from `e55deb3d` to `e8c5fa70` and the full plan against `main`.Round-1 findings are listed. Next I’ll read the two fix commits and the current plan against the migration, runbook, and seed script.The fixes are in. I’ll check them against the migration, the renewal path, and the runbook, then probe the rollback proof on the local database inside a transaction that rolls back.The round-1 fixes are in the text. Next I’ll measure the rollback proof and `g-seed.sh` on the local database, inside a transaction that rolls back, and with a loopback-only seed run.Round-1 production defects are fixed at `e8c5fa70`. The reserve rollback is the inverse of migration `20260925000001`, its failure path exits 3 and leaves the catalog in place, and `g-seed.sh` refuses a short-lived token and a dead read probe without a false pass. Three rigour gaps remain.

Checked on the local database inside transactions that rolled back, and by running `g-seed.sh` only against `127.0.0.1`. Afterward the ledger row `20260925000001`, `swarm.wake_path_release`, and the five unclaimed observed rows were still present. No production host was contacted.

Round-1 items that now hold:

- A credential with under 90 minutes left exits 2 before any CLI or curl call (`g-seed.sh:22-32`). A 30-minute expiry prints `expires in 29 min (< 90); mint again`. Renewal is due only in the last 6 minutes of the stated expiry: the session assumes a 1-hour life (`src/cloud/renewal.ts:739-745`) and the lead is capped at 15 minutes (`src/cloud/renewal.ts:120-146`). The mint window is 20:00–21:30Z for a 21:45Z start (`BOX-SECTION6.md:34-39`).
- Curl to a closed port exits 5 with `HTTP 000`, and `read-headers.txt` is gone. During a live call the file is mode `600` (`g-seed.sh:96-108`). `--max-time 30` is set. The bearer was not in argv, stdout, or stderr.
- The failure table covers search_path, section 5, section 6 health and probes a–h, and `g-seed.sh` exits 2, 4, 5, and 6 (`BOX-SECTION6.md:99-108`). Exit 4 and exit 5 both stop; a 200 that omits note 2 exits 5.
- Rollback runs `assert-database-identity.sh`, stages both files, names the down-migration as a HezLead exception, sets `statement_timeout` 60s, and rolls the edge back before the SQL (`BOX-SECTION6.md:118-127`, `20260925000001-rollback.sql:12`). After step 1 the SQL keeps the widened check9. The site roster returns agents when `agent_wake_path` errors (`site/src/components/app/LiveDashboard.astro:2025-2029`).
- With 5 unclaimed rows the proof is `t` and the restored ACL is `{swarm_admin=X/swarm_admin,authenticated=X/swarm_admin,swarm_read=X/swarm_admin}`. A forced-false proof exits 3 with `rollback catalog proof FAILED` and changes nothing. Before any rollback the proof is `f`. Re-applying the migration in the same transaction recreates the wrapper, inner function, and release table.
- The widened `pg_get_constraintdef` matches the proof literal, and so does the restored pre-G literal. A different check that still contains the old substring returns `f`. An `anon` grant, a dropped comment, a `NOT VALID` check, and a widened check with those rows neutralized all return `f`.
- Step 3 is documented as a health check. Step 1 is the discriminator (`BOX-SECTION6.md:85-91`). The probe limit 50 is what the read edge accepts (`supabase/functions/read/index.ts:334-336`); `cswarm check` pages 20 (`src/cloud/agent-check.ts:30`).
- The mint block, under bash 3.2 and `zsh -f`, prints only `OK`. The renewal sentence is in `mint.log` (mode `600`). The token is not on the terminal or in argv. Run ids are lowercase. Flags match `src/cli.ts:935-937` and the command edge (`supabase/functions/command/index.ts:2458-2472`). `/start` emits `commonswarm:anon-key` through `site/src/layouts/Base.astro:206`.
- This plan replaces the on-box seed paragraph in `deploy/RELEASE-TO-BOX.md:1276-1288` (`BOX-SECTION6.md:7-9`). With `search_path` `"$user", public, extensions` the G catalog proof is `t`; adding `swarm` makes it `f`. The functional proof exits 3 with the two sentences in the rehearsal table.

## Findings

1. **RIGOUR.** `deploy/release-proofs/item-g/20260925000001-rollback-catalog.sql:15-20` still returns `t` for three wrong catalogs. After a complete rollback, `GRANT EXECUTE` to `authenticator` returned `t`, and the same grant to `service_role` returned `t`. `ALTER FUNCTION ... STABLE` returned `t` (the proof checks `prosecdef` and not `provolatile`). A comment that keeps the prefix `Author-scoped directed receipts` and replaces the rest returned `t`. A missing comment, a wholly different comment, an `anon` grant, and `PUBLIC` all returned `f`. The rollback script itself restores the pre-G ACL and the original volatile definition.

2. **RIGOUR.** `docs/evidence/2026-09-25-item-g-lane1/BOX-SECTION6.md:81-83` says the step-1 stop gate reads `ack_outcome: observed`. `cswarm receipt --json` writes that value as `outcome` (`src/cloud/receipts.ts:290`), and `g-seed.sh:85` checks `outcome`. The rehearsal row at `BOX-SECTION6.md:146` uses `outcome`. The script’s exit 4 is the gate that runs.

3. **RIGOUR.** `g-seed.sh` can exit 1, and `BOX-SECTION6.md:99-108` has no row for it. The header at `g-seed.sh:8-11` lists 0, 2, 4, 5, and 6. Measured exits 1: fewer than four arguments (`g-seed.sh:13`, `$1: unbound variable`), and a recipient file with no `principal_id` (`g-seed.sh:35`, `KeyError`, before any call). Exit 5’s row (`BOX-SECTION6.md:106`) names a 5xx, a wrong body, and a connection failure. An HTTP 401 or 400 is an exit 5 whose action is not stated. With the 90-minute guard, that status is not the expiry path from round 1.

VERDICT: PASS
