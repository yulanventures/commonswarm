You review one lane of the CommonSwarm repo as one arm of a D-036 pair. You are a reviewer: change no file, commit
nothing. HARD RULES: contact no production host (api.commonswarm.com, commonswarm.com, edge-staging.commonswarm.com,
178.105.29.28, 100.115.66.74); never run a cswarm command without a loopback --url; no ssh, vercel, no remote supabase
command; no db:reset, db:stop, supabase stop/start, and do not run test:p1-server (the lead owns the local stack);
do not read ~/.cswarm or ~/.config/cswarm; start no other model; use no skill; print no secret. Read-only git and
file reads; pure tests with a temporary HOME (build first for dist/ tests). Kill every process you start.

LANE: item L (exactly-once file_put and brain_put over MCP; CLI --request-id), branch lane/item-l at 444db2b7. Your
directory is a detached worktree at 444db2b7. Base on main: 084f8a22. Spec:
docs/design/2026-09-25-ITEM-L-EXACTLY-ONCE-FILE-PUT-BRIEF.md. Maker: Codex gpt-6-sol. Record:
docs/evidence/2026-09-25-item-l/LANE.md (Fold 1 included). The lead ran the file-artifacts server test on the local
stack: 22/22 at 444db2b7 (local storage-api v1.54.1 answers a duplicate PUT with HTTP 400 and body statusCode "409",
error "Duplicate"; production runs v1.77.5, whose shape is not measured; Fold 1 makes the commit, not storage's error
shape, the source of truth on a replayed PUT).

REVIEW `git diff 084f8a22..444db2b7` (skip docs/design). CHECK with file:line evidence:
1. Exactly-once: one request_id + same content gives at most one committed version under every failure (lost response
   after create / PUT / commit, killed process between phases, deleted resume record, concurrent callers with the same
   request_id, a URL that expired after 2 hours, a pending slot swept after 3 hours). Can any path create a second
   version, or report "replayed" or "committed" falsely? Is the upload path really per version (quote the create
   handler)? Is the derived identity collision-safe (name case, workspace, principal, request_id format)?
2. Same request_id + different content: typed conflict, no second version; and the stated behaviour when the local
   record is gone.
3. The durable record: 0600 in a 0700 directory, bounded, holds no secret, safe under concurrent processes (the lock),
   and never blocks a fresh request_id.
4. MCP tools: inputs (absolute path read by the server process; no bytes or URL or token in the model turn or the
   output), the 25 MiB cap and type refusal before any network call, brain_put if_version typed and replay-safe,
   outputs; the command table, MCP tool list and generated help agree; the removed "until item L" marker is gone from
   every copy.
5. CLI --request-id parity; without it, behaviour unchanged.
6. Every claimed control fails on revert (read test and code; run the pure tests); every changed test file is in a gate;
   every LANE.md claim is true.

OUTPUT: findings with file:line, severity PRODUCTION or RIGOUR, evidence (quote the code). Final line exactly:
VERDICT: PASS  or  VERDICT: FAIL
