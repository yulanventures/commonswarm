### Findings

#### PRODUCTION
*None.* Direct evaluation of the command dispatch architecture, baseline matrices, and mutation controls reveals no undeclared deviation in exit code, `stdout`, or `stderr` against `main` (`9543527b10f308e932f98f9124da7f95fa63e26f`). The prototype lookup vulnerability on closed group sub-actions identified in Round 5 is properly closed by `Object.hasOwn(root.subcommands, action)`, and the resulting behavior changes on the 36 `cswarm channel <Object.prototype name>` rows and 3 server file refusal rows after `--` are declared.

---

#### RIGOUR

##### 1. Contradiction on Exit Code Invariance in `DECLARED-CHANGES.md`
- **Diff Hunk**:
  ```diff
  --- a/docs/evidence/2026-09-16-h-lane1/DECLARED-CHANGES.md
  +++ b/docs/evidence/2026-09-16-h-lane1/DECLARED-CHANGES.md
  @@ -1,6 +1,8 @@
   # Declared command-baseline changes
   
  +All rows below keep their exit code. They change only where the lane 1 spec or
  +the item-L ruling requires it.
  ```
  and
  ```diff
  +## Own-property sub-action lookup (declared in the conversion commit)
  +
  +The conversion commit selects a sub-action only when the group's own
  +`subcommands` object has it. On `4cb8c5fe`, `runChannel` looked the name up in
  +`CHANNEL_SUBCOMMANDS` and called what it inherited from `Object.prototype`. For
  +each of the 12 `Object.prototype` names on the plain, JSON, and valid-profile
  +axes (36 rows, `refusal.prototype-sub-action.channel.*`), the old result was
  +exit 0 with no output (`toString`, `constructor`), exit 1 with "chosen is not a
  +function" (`__proto__`), or exit 1 with a TypeError (the rest). The new result
  +is exit 1 with the channel refusal and usage, byte-identical to
  +`refusal.group.channel.unknown.*`.
  ```
- **Description**: Lines 3–4 of [DECLARED-CHANGES.md](file:///docs/evidence/2026-09-16-h-lane1/DECLARED-CHANGES.md#L3-L4) state: *"All rows below keep their exit code."* However, Section 2 ([DECLARED-CHANGES.md](file:///docs/evidence/2026-09-16-h-lane1/DECLARED-CHANGES.md#L141-L157)) explicitly documents that for `toString` and `constructor` across the three test axes (`plain`, `json`, and `valid-profile`), the previous behavior on `main` was `exit 0` with no output, whereas the converted lane yields `exit 1` with the channel unknown-action refusal. Those 6 rows (`refusal.prototype-sub-action.channel.*.{toString,constructor}`) do change exit code from 0 to 1, directly contradicting the blanket assertion at the beginning of the file.

##### 2. Stale Evidence in `SCAN.txt` Preserving Obsolete Round 5 Code and Pre-Rebase Line Numbers
- **Diff Hunk**:
  ```diff
  --- a/docs/evidence/2026-09-16-h-lane1/SCAN.txt
  +++ b/docs/evidence/2026-09-16-h-lane1/SCAN.txt
  @@ -0,0 +1,76 @@
  +Command run once:
  +
  +rg -n '(^|[^A-Za-z])(verb|action|command|subcommand) (===|!==)|\.includes\(' src/cli.ts src/onboarding-cli.ts src/cloud
  +
  +Output:
  ...
  +src/cli.ts:9338:  const entry = action === undefined ? undefined : root.subcommands[action];
  ```
- **Description**: [SCAN.txt](file:///docs/evidence/2026-09-16-h-lane1/SCAN.txt#L35) records line 9338 of `src/cli.ts` as `const entry = action === undefined ? undefined : root.subcommands[action];`. This is the exact inherited property read from Round 5 that caused the production prototype pollution defect. In Round 6 commit 2, this was replaced by the own-property check `Object.hasOwn(root.subcommands, action)`, which [CONTROLS.md](file:///docs/evidence/2026-09-16-h-lane1/CONTROLS.md#L66-L71) proves is pinned by the allowlist. Furthermore, the check-budget rebase onto `9543527b` shifted citations in `src/cli.ts`. Citing the pre-fix line 9338 in `SCAN.txt` creates contradictory audit evidence.

##### 3. Omission of Unchanged Variant Axes for `receive.status` in `DECLARED-CHANGES.md`
- **Diff Hunk**:
  ```diff
  --- a/docs/evidence/2026-09-16-h-lane1/DECLARED-CHANGES.md
  +++ b/docs/evidence/2026-09-16-h-lane1/DECLARED-CHANGES.md
  @@ -53,6 +53,10 @@
   - `selected-error.receive.serve.profile-json-missing-before`
   - `selected-error.receive.serve.host-before`
   - `selected-error.receive.status.json-before`
   - `selected-error.receive.status.profile-missing-before`
   - `selected-error.receive.status.json-profile-missing-before`
   - `selected-error.receive.status.profile-json-missing-before`
   - `selected-error.receive.test.json-before`
  ```
- **Description**: While sibling onboarding commands (`check`, `setup`, and all other `receive.*` modes) list all 8 matrix permutations generated by `selectedErrorFixtures()`, `receive.status` lists only 4 rows (omitting `profile-valid-before`, `json-profile-valid-before`, `profile-json-valid-before`, and `host-before`). While this is mathematically consistent with the 129 changed rows in Commit 3, [DECLARED-CHANGES.md](file:///docs/evidence/2026-09-16-h-lane1/DECLARED-CHANGES.md) lacks an explicit note explaining why these 4 valid-profile/host axes are unchanged controls rather than selected error transitions.

---

### External Dependencies (Not Inlined in Part B4)
As required by Part B4 instructions, the following dependencies cannot be directly evaluated in this review step:
1. `src/cli.ts` implementation details (AST statement count verification, `selectCommandEntry` line implementation, and turn-hook exit logic).
2. `src/onboarding-cli.ts` check-budget deadlines and process exit hooks.
3. `tests/p1-cli/command-table-gates.test.ts` test logic and gate definitions.
4. `tests/p1-cli/command-dispatch-baseline.test.ts` harness execution.

---

VERDICT: PASS
The baseline counts (1248 rows: 35 exit 0, 1213 non-zero) and 129 declared baseline changes are mathematically consistent; the Round 5 prototype defect on closed groups is resolved with own-property guards and pinned by mutation gates, with no production regressions identified.
