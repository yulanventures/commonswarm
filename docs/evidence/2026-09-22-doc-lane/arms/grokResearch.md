I'll search the tree for every claim the move made false, starting with the files and terms you named, and I will not edit anything or touch a live host.The first pass shows live deploy steps still point at the deleted Supabase project and the deleted Vercel site. I'll read those files in full and keep only claims that are false now.The cutover runbooks still say the move has not run. I’ll read the operator docs, legal pages, and the strings tests pin, and I’ll leave history files out.76 statements are now false. The rows that would send an agent to the deleted Supabase project or the deleted Vercel project, or that would tell a customer the wrong host, are first.

`CONTRIBUTING.md` is not in this tree. `CLAUDE.md` does not state a host of its own. `package.json` `db:*` scripts still run the local Supabase CLI. `brain-shared-host.md` has no production-host claim. Local `npm run db:start`, the `supabase/` migrations, and `https://api.commonswarm.com` as the API name stay true.

| File:line | Exact current text (short) | Why it is false now | Category | Replacement |
|---|---|---|---|---|
| AGENTS.md:313-316 | The Supabase project `cloud-swarm-dev` (ref `ukezjcnxjvkpkeezxaew`) is the CommonSwarm production project; … `https://ukezjcnxjvkpkeezxaew.supabase.co` serve the same project. | That project was deleted on 2026-09-20. `api.commonswarm.com` is Caddy on the Hetzner server, not that project. | OPERATOR-INSTRUCTION | Production is one Hetzner server, yulan-vps-1, in Falkenstein. The Supabase project `cloud-swarm-dev` is deleted. Do not use that ref. |
| AGENTS.md:318 | Resolve the ref against the live page before `supabase db push`, `functions deploy`, or any destructive operation. | Those commands talk to the deleted project. The way a migration or a new function reaches the server is not decided. | OPERATOR-INSTRUCTION | needs a decision: how a schema migration reaches the production database, and how a new edge-function version is deployed beyond the release directory and `current` symlink. |
| AGENTS.md:323-324 | Vercel project: `coswarm-site`; … public project alias: `coswarm-site.vercel.app`. | The Vercel project is deleted. The site is static files from Caddy on the same server. The Vercel team is on Hobby. | OPERATOR-INSTRUCTION | The website is static files served by Caddy on yulan-vps-1. Publish with `deploy/site/deploy.sh`. Do not use Vercel. |
| AGENTS.md:327 | the anon key from `supabase projects api-keys` | That command calls the Supabase Management API for a project that does not exist. | OPERATOR-INSTRUCTION | Take the anon key from the server environment or from the `commonswarm:anon-key` meta tag. Do not run `supabase projects api-keys`. |
| AGENTS.md:332-333 | `cp -r .vercel dist/.vercel` / `vercel deploy dist --prod --yes --scope ridgedotio` | This deploys to the deleted project, or can create a new one. | OPERATOR-INSTRUCTION | Build the site, then run `deploy/site/deploy.sh yulan-vps-1`. Check https://commonswarm.com. |
| AGENTS.md:338 | Per-deploy URLs can return 302 to Vercel SSO | There is no Vercel deploy URL. | OPERATOR-INSTRUCTION | remove |
| AGENTS.md:340 | copy `.vercel` into rebuilt `dist/` or deployment can silently target a new `dist` project | This is the Vercel project-link step. Vercel is not the host. | OPERATOR-INSTRUCTION | remove |
| AGENTS.md:65-66 | Do not rename the Vercel project or alias `coswarm-site` | There is no Vercel project to keep. | OPERATOR-INSTRUCTION | Remove the Vercel clause. Keep the warning not to rename `__COSWARM_VERSION__`. |
| AGENTS.md:56 | a hosted Supabase backend | Production is not Supabase's hosted platform. | OPERATOR-INSTRUCTION | a backend on one Hetzner server in Falkenstein, and the website at https://commonswarm.com on that same server. |
| README.md:103 | target either local or hosted Supabase | "Hosted Supabase" was the deleted project. | OPERATOR-INSTRUCTION | Target the local Supabase CLI, or the production API at https://api.commonswarm.com. |
| brain-releases.md:62-64 | `cp -r .vercel dist/.vercel` … `vercel deploy dist --prod --yes --scope ridgedotio` | This is the live release ritual, and it deploys to the deleted Vercel project. | OPERATOR-INSTRUCTION | Publish the site with `deploy/site/deploy.sh yulan-vps-1`, then check https://commonswarm.com. |
| brain-releases.md:90 | `supabase db push --linked` → verify `migration list` remote column | `--linked` targets the deleted project. | OPERATOR-INSTRUCTION | needs a decision: how a schema migration reaches the production database. |
| brain-releases.md:152-154 | `supabase db push --linked --dry-run` must name exactly the files… run the push from the linked checkout | An agent would push to the deleted project. | OPERATOR-INSTRUCTION | needs a decision: how a schema migration reaches the production database. |
| brain-releases.md:136 | the direct deployment URL 302s to SSO | That 302 is a Vercel per-deploy URL. The site is not published that way. | OPERATOR-INSTRUCTION | Check https://commonswarm.com after the server site deploy. Do not use a Vercel deployment URL. |
| deploy/site/RUNBOOK.md:3 | Status: **PRODUCTION CUTOVER NOT RUN**. … Production DNS still points to Vercel. | The site has been on the server since 2026-09-16 23:01Z. The Vercel project is deleted. | OPERATOR-INSTRUCTION | Status: the site cutover is done. https://commonswarm.com is served by Caddy on yulan-vps-1. |
| deploy/site/RUNBOOK.md:87 | For a full rollback to Vercel, restore the former Cloudflare DNS records | The Vercel project is deleted. That rollback cannot work. | OPERATOR-INSTRUCTION | Roll back a site release on the server by moving the `current` symlink. Do not point DNS at Vercel. |
| deploy/site/RUNBOOK.md:91-98 | Delete the Vercel `coswarm-site` project only after… Do not delete it before this confirmation. | The project is already deleted. | OPERATOR-INSTRUCTION | remove |
| deploy/edge-runtime/RUNBOOK.md:11-13 | production DNS, the Management API custom-domain deactivation, production controls, and rollback have not run | API DNS already goes to the server. The Supabase project and its custom domain are deleted. | OPERATOR-INSTRUCTION | The API cutover is done. Do not deactivate a Supabase custom domain. needs a decision: how a new edge-function version is deployed beyond the release directory and `current` symlink. |
| deploy/edge-runtime/RUNBOOK.md:160-162 | Auth, REST, Storage, and Realtime reach the project URL with the upstream Host … rewritten to `ukezjcnxjvkpkeezxaew.supabase.co` | Those paths go to containers on the server, not to the deleted project. | OPERATOR-INSTRUCTION | Check Auth, REST, Storage, and Realtime on the server containers. Do not rewrite Host to the deleted supabase.co name. |
| deploy/edge-runtime/RUNBOOK.md:181-184 | sends Auth, REST, Storage, and Realtime to the project host | Same false route. | OPERATOR-INSTRUCTION | Caddy sends those paths to the local containers, as in `deploy/supabase-stack/commonswarm-api.caddy`. |
| deploy/edge-runtime/RUNBOOK.md:196-199 | Deactivate the Supabase custom domain through the Supabase Management API | The project is deleted. This call hits the wrong system. | OPERATOR-INSTRUCTION | remove |
| deploy/edge-runtime/RUNBOOK.md:205-207 | keep `https://ukezjcnxjvkpkeezxaew.supabase.co` and its hosted functions available as the fallback | That host is gone. It is not a fallback. | OPERATOR-INSTRUCTION | remove |
| deploy/edge-runtime/RUNBOOK.md:213-218 | record … counts … in the Supabase Edge Functions dashboard … invocations at Supabase should fall to zero … traffic continues at the project origin | There is no Supabase dashboard and no project origin. Those services are on the server. | OPERATOR-INSTRUCTION | remove |
| deploy/edge-runtime/RUNBOOK.md:229-231 | rollback is only restoring the DNS-only CNAME `ukezjcnxjvkpkeezxaew.supabase.co` | That name does not serve production. | OPERATOR-INSTRUCTION | remove |
| deploy/edge-runtime/VERIFICATION.md:11-14 | Production DNS, Management API custom-domain deactivation, production controls, and rollback have not run. | The API cutover is done. The project is deleted. | OPERATOR-INSTRUCTION | The API cutover is done. This "not established" item is closed. |
| deploy/edge-runtime/README.md:73-77 | Caddy destination … Supabase project origin | Auth, REST, Storage, and Realtime are local containers behind Caddy. | OPERATOR-INSTRUCTION | Caddy destination: the matching container on the server. |
| deploy/edge-runtime/README.md:79-80 | The reverse proxy sends the project origin as HTTP `Host`, `X-Forwarded-Host`, and TLS SNI. | That origin is the deleted project. The live API Caddy file does not do this. | OPERATOR-INSTRUCTION | remove for the live API file. The live routes are in `deploy/supabase-stack/commonswarm-api.caddy`. |
| deploy/edge-runtime/README.md:108-118 | End-to-end latency is still slower than production. … The named remedy is **N-db**. A production timeout after cutover means DNS rollback | The server is production. N-db has landed. A rollback to the deleted project is not a remedy. | OPERATOR-INSTRUCTION | Remove the comparison to the old hosted project. Do not roll DNS back to supabase.co. |
| deploy/supabase-stack/RUNBOOK.md:1 | N-db cutover runbook — NOT RUN | The hosted project was deleted on 2026-09-20. Production is the server. | OPERATOR-INSTRUCTION | Status: the database cutover is done. Do not run this file against a source project. |
| deploy/supabase-stack/RUNBOOK.md:3 | This lane deployed nothing. HezLead runs every command in this file. | Running it now would read the deleted project. | OPERATOR-INSTRUCTION | remove |
| deploy/supabase-stack/RUNBOOK.md:111 | set `SOURCE_STORAGE_URL=https://ukezjcnxjvkpkeezxaew.supabase.co/storage/v1` | That Storage API is gone. Files are in Cloudflare R2 through the server Storage API. | OPERATOR-INSTRUCTION | remove |
| deploy/supabase-stack/RUNBOOK.md:313 | any client that calls `ukezjcnxjvkpkeezxaew.supabase.co` … read the Supabase API logs | The host is deleted. There are no Supabase API logs. | OPERATOR-INSTRUCTION | remove |
| deploy/supabase-stack/RUNBOOK.md:386 | Keep the hosted project frozen for 48 hours. Do not unfreeze the hosted project. | There is no hosted project to freeze. | OPERATOR-INSTRUCTION | remove |
| deploy/supabase-stack/RUNBOOK.md:443 | Adding a redirect URI does not change hosted auth. | There is no hosted auth. Sign-in is GoTrue on the server. | OPERATOR-INSTRUCTION | Sign-in is GoTrue on the server: GitHub (yulanventures OAuth app), Google, and email. SMTP is Resend. |
| deploy/supabase-stack/VERSIONS.md:13 | Before the box rehearsal, the lead repeats the available public version checks. If a published production version differs… from a fresh dump | This compares the server to the old hosted project and dumps that project. | OPERATOR-INSTRUCTION | needs a decision: how image pins are checked now that production is the server, not a hosted project. |
| docs/design/WEB-ONBOARDING.md:139-140 | `supabase functions deploy command --project-ref <ref>` and the same for `read` | This deploys to the deleted project. The doc still says the sequence is the deploy runbook. | OPERATOR-INSTRUCTION | needs a decision: how a new edge-function version is deployed beyond the release directory and `current` symlink. |
| docs/design/WEB-ONBOARDING.md:181-191 | In the Supabase **dashboard** for the production project… only GitHub… plus `https://coswarm-site.vercel.app/app` while that alias is still served. | There is no dashboard and no Vercel alias. Sign-in is GitHub, Google, and email. | OPERATOR-INSTRUCTION | Change GoTrue settings on the server. Do not open the Supabase dashboard. |
| docs/design/WEB-ONBOARDING.md:231-243 | `vercel deploy dist` … Environment variables set in the Vercel dashboard have no effect | The site is not deployed to Vercel. | OPERATOR-INSTRUCTION | Build with `PUBLIC_SUPABASE_URL=https://api.commonswarm.com` and the anon key, then run `deploy/site/deploy.sh yulan-vps-1`. |
| docs/design/WEB-ONBOARDING.md:268 | `supabase secrets set SWARM_SELF_SERVE=1 --project-ref <ref>` | This sets a secret on the deleted project. | OPERATOR-INSTRUCTION | needs a decision: where the edge runtime reads `SWARM_SELF_SERVE` on the server. The value in production is still 1. |
| docs/design/2026-09-04-GOOGLE-SIGNIN.md:5 | Google is **off** in the Supabase dashboard, so no build can show a Google button today. | Google sign-in is on. There is no Supabase dashboard. | OPERATOR-INSTRUCTION | Google, GitHub, and email are enabled on the server GoTrue. |
| docs/design/2026-09-04-GOOGLE-SIGNIN.md:229 | the deploy in AGENTS.md … (`vercel deploy dist`) | This points at the false site deploy. | OPERATOR-INSTRUCTION | The site deploy is `deploy/site/deploy.sh`. |
| docs/design/2026-09-04-GOOGLE-SIGNIN.md:242 | **Supabase dashboard first, then rebuild and deploy.** | The dashboard is gone. | OPERATOR-INSTRUCTION | Change GoTrue on the server, then rebuild the site and publish it with `deploy/site/deploy.sh`. |
| docs/design/2026-09-04-GOOGLE-SIGNIN.md:588 | `https://supabase.com/dashboard/project/ukezjcnxjvkpkeezxaew/auth/providers?provider=Google` | That project is deleted. | OPERATOR-INSTRUCTION | remove |
| docs/design/2026-09-04-GOOGLE-SIGNIN.md:634 | `ukezjcnxjvkpkeezxaew` **is production.** There is no separate CommonSwarm production project. | The project is deleted. | OPERATOR-INSTRUCTION | remove |
| docs/design/2026-09-04-GOOGLE-SIGNIN.md:646-649 | `cp -r .vercel dist/.vercel` / `vercel deploy dist --prod --yes --scope ridgedotio` | This deploys to the deleted Vercel project. | OPERATOR-INSTRUCTION | Publish with `deploy/site/deploy.sh yulan-vps-1`. |
| site/README.md:179 | `vercel deploy dist --prod --yes --name coswarm-site --scope ridgedotio` | Same deleted project. | OPERATOR-INSTRUCTION | Publish with `deploy/site/deploy.sh yulan-vps-1`. |
| TODO.md:44 | `https://coswarm-site.vercel.app` still 200, so nothing broke. | The project is deleted. That URL is not a live alias. | OPERATOR-INSTRUCTION | remove |
| TODO.md:49-50 | the Vercel **project** is still named `coswarm-site`. Renaming it moves the deployment URL | The project is deleted, not waiting to be renamed. | OPERATOR-INSTRUCTION | remove |
| TODO.md:86-87 | The Vercel alias still returns 200 and the Vercel project keeps its `coswarm-site` name | Both claims are false. | OPERATOR-INSTRUCTION | remove |
| TODO.md:213-216 | the region of the production Supabase project and **lives in the Supabase dashboard** | The database is on the server in Falkenstein, Germany. There is no dashboard region. | OPERATOR-INSTRUCTION | The production database is in Falkenstein, Germany. Do not read a Supabase dashboard. |
| TODO.md:321 | Supabase hosting region … East US (North Virginia) | The database is not in East US. | OPERATOR-INSTRUCTION | The production database is in Falkenstein, Germany. |
| TODO.md:327 | Supabase custom SMTP is active at 30 emails/hour | Mail is Resend SMTP into GoTrue on the server. There is no Supabase project SMTP setting. The 30-per-hour cap is not established for the server. | OPERATOR-INSTRUCTION | Sign-in email uses Resend SMTP on the server. The send rate on the server is not established. |
| site/src/pages/privacy.astro:54-58 | the hosted project's region lives in the Supabase dashboard | An editor would look at a deleted dashboard and write the wrong region. | OPERATOR-INSTRUCTION | The database region is Falkenstein, Germany. It is not a dashboard field. |
| site/src/lib/commonswarm.ts:296-298 | With no custom SMTP configured, the built-in sender permits 2 emails per hour for the ENTIRE PROJECT | Production mail is Resend, not the Supabase built-in sender. | OPERATOR-INSTRUCTION | Sign-in email is sent through Resend. Do not describe the Supabase built-in 2-per-hour sender as production. |
| site/src/layouts/Base.astro:91 | Vercel builds set them; that is the ordinary case | Ordinary builds are not Vercel builds. | OPERATOR-INSTRUCTION | The site build on the operator machine sets them, then `deploy/site/deploy.sh` publishes the files. |
| docs/design/2026-08-18-FILE-ARTIFACTS.md:392 | the deploy checklist in AGENTS.md | That checklist is the Vercel and `db push` procedure. | SPEC | needs a decision: how a schema migration, an edge-function version, and a site build reach the server. |
| docs/design/SWARM-CLOUD.md:24-25 | the Vercel project alias still serves underneath | It does not. The project is deleted. | SPEC | https://commonswarm.com is the public site, served by Caddy on the Hetzner server. The Vercel alias is gone. |
| docs/design/CAPABILITY-URL-ONRAMP.md:61 | `https://coswarm-site.vercel.app/see#swm_cap_...` | That host is deleted. The CLI still prints it as the default. | SPEC | The default link host is https://commonswarm.com/see#... |
| docs/design/CAPABILITY-URL-ONRAMP.md:92 | may name only `coswarm-site.vercel.app` (the default) | The default is a deleted host. | SPEC | The default host is commonswarm.com. Remove coswarm-site.vercel.app from the allowlist. |
| site/src/pages/privacy.astro:222 | These, and only these, are in the path: | The list that follows names Supabase and Vercel and leaves out the real hosts. | PRODUCT/LEGAL | Keep this sentence only after the list names the real processors: the server we operate, Cloudflare, and Resend. |
| site/src/pages/privacy.astro:251 | **Supabase.** Hosting for the database… The production database is hosted in the United States (East US, North Virginia). | The database, GoTrue, and functions run in Falkenstein, Germany. Files are in Cloudflare R2. Supabase Cloud does not host them. | PRODUCT/LEGAL | The database, sign-in service, and server functions run on a server we operate in Falkenstein, Germany. Files are stored in Cloudflare R2. The R2 region is not established. |
| site/src/pages/privacy.astro:252 | **Vercel.** Hosting for this website only. Vercel serves static pages… | The website is Caddy on the same server. Vercel is not in the path. | PRODUCT/LEGAL | remove |
| site/src/pages/privacy.astro:315 | our providers store data there or in the region noted above. … your data is transferred to and processed in the United States | The database is in Germany, not in East US. | PRODUCT/LEGAL | The company is in the United States. The database is in Germany. The file-storage region is not established. Do not say the data is processed in East US. |
| site/src/pages/privacy.astro:318 | Transfers to the United States rely on the European Commission's and the UK's standard contractual clauses as implemented by our providers. | This assumes Supabase and Vercel store the data in the US under their clauses. | PRODUCT/LEGAL | needs a decision: which transfer terms to state now that the database is on our server in Germany. |
| SECURITY.md:33 | Out of scope: vulnerabilities in GitHub, Supabase, or Vercel themselves | Vercel does not host the site. Supabase Cloud does not host the service. | PRODUCT/LEGAL | Out of scope: vulnerabilities in GitHub, Cloudflare, Hetzner, Resend, or the upstream images we run. This service is not on Supabase Cloud or Vercel. |
| SECURITY.md:61-66 | https://CommonSwarm-site.vercel.app/terms (and privacy, acceptable-use). No permanent domain has been decided | The domain is https://commonswarm.com. Those Vercel URLs are not the site. | PRODUCT/LEGAL | Link to https://commonswarm.com/terms, /privacy, and /acceptable-use. Remove the sentence that no domain has been decided. |
| src/cli.ts:995-998 | accepts only https://coswarm-site.vercel.app (the default) | The default link points at a deleted host. | CODE string | The default is https://commonswarm.com. Remove coswarm-site.vercel.app from the accepted hosts. |
| src/cloud/capability-link.ts:8 | `CAPABILITY_SITE_ORIGIN = "https://coswarm-site.vercel.app"` | Same dead default. The error text uses this example. | CODE string | Set the default to https://commonswarm.com. |
| src/cloud/capability-link.ts:16-21 | `coswarm-site.vercel.app` is the Vercel project alias and stays until DNS moves. | DNS has moved and the project is deleted. The allowlist still contains that host. | CODE string | Remove that host from `CAPABILITY_ALLOWED_HOSTS`. |
| src/cloud/config.ts:16 | a self-hosted deployment uses its Supabase project base URL (`https://<ref>.supabase.co`) … if you created it | This tells the user to create or use a Supabase Cloud project. Production is https://api.commonswarm.com. | CODE string | A deployment uses its own base URL. The service we run is https://api.commonswarm.com. |
| src/cloud/config.ts:27 | `--url must be the Supabase project base URL` | The check is "no path". The words say the target must be a Supabase project. | CODE string | `--url` must be the service base URL, with no path. |
| src/cloud/current-target.ts:327 | save its Supabase project base URL … `https://<ref>.supabase.co` … if you created it | Invite links save https://api.commonswarm.com, not a supabase.co project. | CODE string | Invite links save the service base URL. The service we run is https://api.commonswarm.com. Do not tell the user to create a Supabase project. |
| src/cloud/invite-link.ts:24-30 | Both origins serve the SAME production project … the supabase.co host keeps working … `https://ukezjcnxjvkpkeezxaew.supabase.co` | That host is deleted. It does not serve production. | CODE string | The only production API origin is https://api.commonswarm.com. Remove the supabase.co origin. |
| tests/p1-cli/cli-errors.test.ts:41-42,80 | requires `/Supabase project base URL/` and `https://<ref>.supabase.co` in the error | This pins the false remedy. | TEST pin | Pin the new sentence. Do not require `<ref>.supabase.co`. |
| tests/p1-cli/accept-link.test.ts:300-308 | the supabase.co host still serves the same project … `https://ukezjcnxjvkpkeezxaew.supabase.co` must pass silently | This pins the deleted host as a production origin. | TEST pin | Require silent accept only for https://api.commonswarm.com. The deleted host must not pass as production. |
| tests/p1-cli/edge-runtime-box.test.ts:467-471 | `upstreamHost = "ukezjcnxjvkpkeezxaew.supabase.co"` and `header_up Host` must match | This pins Caddy to the deleted project. The live API Caddy file sends those paths to local ports. | TEST pin | Pin the live API routes to the local containers. Do not require the deleted host. |

Not given a row, because they are outside the named places: `supabase/functions/command/cors.ts:4` and `supabase/functions/capability/index.ts:186` still allow `https://coswarm-site.vercel.app`. `deploy/edge-runtime/commonswarm.caddy` still proxies Auth, REST, Storage, and Realtime to `ukezjcnxjvkpkeezxaew.supabase.co`. The test that pins that Caddy file is the last row.

Kept on purpose: local `npm run db:start` at `127.0.0.1:54321`; `supabase/` as the migration and function source; "hosted service" where it means the service this company runs; `tests/p1-cli/supabase-stack.test.ts`, which rejects the old GitHub callback; `tests/p1-cli/f1-target-discovery.test.ts`, which uses the old URL only as sample HTML and still requires `DEFAULT_SITE_ORIGIN` to be `https://commonswarm.com`; `deploy/supabase-stack/commonswarm-api.caddy`, which already uses local ports. Dated `docs/org/*` and `docs/evidence/**` were not listed. Struck sentences already marked dead were not listed again. Dated lane plans were not listed unless they still say to do the old deploy now.

Files in the re-run set: **546**. `CONTRIBUTING.md` was checked and is absent (not counted). Counts: 8 named files present (`AGENTS.md`, `CLAUDE.md`, `README.md`, `SECURITY.md`, `TODO.md`, `package.json`, `site/package.json`, `site/README.md`), 82 `docs/design/**/*.md`, 13 `deploy/**/*.md`, 165 `site/src/**`, 102 `src/**/*.ts`, 174 `tests/**/*.{ts,mjs,js}`, plus the two brain files.

Re-run from the repo root:

```sh
rg -n -g '{AGENTS.md,CLAUDE.md,README.md,SECURITY.md,TODO.md,CONTRIBUTING.md,package.json}' 'supabase|vercel|Vercel|hosted|ukezjcnxjvkpkeezxaew|cloud-swarm-dev|db push|functions deploy|--linked|coswarm-site|us-east|Kong|dashboard'
rg -n -g 'site/package.json' -g 'site/README.md' 'supabase|vercel|Vercel|hosted|ukezjcnxjvkpkeezxaew|cloud-swarm-dev|db push|functions deploy|coswarm-site'
rg -n -g 'docs/design/**/*.md' 'supabase|vercel|Vercel|hosted|ukezjcnxjvkpkeezxaew|cloud-swarm-dev|db push|functions deploy|--linked|coswarm-site|us-east|dashboard|project-ref'
rg -n -g 'deploy/**/*.md' 'supabase|vercel|Vercel|hosted|ukezjcnxjvkpkeezxaew|cloud-swarm-dev|db push|functions deploy|NOT RUN|cutover|Kong|dashboard'
rg -n -g 'site/src/**/*.{astro,ts,mjs,js}' 'supabase|vercel|Vercel|ukezjcnxjvkpkeezxaew|coswarm-site|East US|North Virginia|United States|Resend|dashboard'
rg -n -g 'src/**/*.ts' 'supabase\.co|vercel\.app|coswarm-site|ukezjcnxjvkpkeezxaew|Supabase project|hosted Supabase'
rg -n -g 'tests/**/*.ts' 'coswarm-site\.vercel\.app|ukezjcnxjvkpkeezxaew|Supabase project base URL|North Virginia|vercel deploy|still serves the same project'
```

The two brain files were read in full at the paths in the task. They are outside this git tree, so `rg` from the repo root does not see them.

RESEARCH COMPLETE: 76 rows
