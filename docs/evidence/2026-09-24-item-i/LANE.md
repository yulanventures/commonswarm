# Item I release 1 implementation lane (2026-09-24)

## Built against the six decisions

1. Setup now requires an explicit `--host-session-id <id>` or `--host-session-id manual`. It records `host_session_id` for a named session and leaves manual profiles unbound. The reader accepts the original six keys, those keys with `workspace_name`, and either form with `host_session_id`; a bound id must be a string of 1–200 characters. cswarm reads no environment variable for the session id. An older CLI will reject the new key as a damaged profile, so the release note must say: **bound profiles need cswarm 0.1.75 or newer**.
2. `requireProfileHost` in `src/cloud/agent-profile.ts` is the single comparison. `readAgentProfile` calls it for all profile openers, before credential, message cache, receive state, or network use. B gets `profile_other_session` with the specified sentence; an omitted id gets `host_session_required` with its distinct sentence. Unbound profiles keep their prior behavior. `check --hook` also presents the hook stdin `session_id` before opening receive state.
3. Setup checks an existing profile before making its identity request. A different id, including `manual`, cannot rebind an existing bound profile. Moving a seat requires the operator to remove the profile directory and set it up again.
4. The connection token repair copy, setup CLI errors, quick guide, setup guide, and site connect prompt give the operator step and show explicit setup commands. The prompt names Claude Code's `CLAUDE_CODE_SESSION_ID` and Codex's `CODEX_THREAD_ID`; both examples rely on the agent's shell to expand the variable. This lane did not establish equality between those shell values and live hook stdin ids, or their behavior after host resume. No other session-id variable is named.
5. MCP checks the profile at startup, before listing managed contexts or creating a transport. Fold 1 changed its error table: `profile_other_session` says stop and tell the operator; `host_session_required` says restart this MCP server with the current host session. A static host MCP configuration's ability to pass a per-session ID remains unestablished; the Claude Code live run must check it before claiming MCP works for a bound profile.
6. No server, migration, read edge, managed proof fence, or production service was changed.

## Before and after local control

- Before binding, an old-format six-key profile invoked as session B with `check --message-id` reaches `message_not_cached`. This is a local cache path with no network request, and demonstrates the old format has no host comparison. The original reader in `HEAD` had no `host_session_id` key or comparison.
- After binding the same temporary profile to A, that B command returns `profile_other_session` before the cache read. A passes the host check and reaches the fake fetcher in the network-path test; an omitted ID returns `host_session_required` before the credential and cache reads. Each CLI process had a three-second timeout and a temporary HOME.
- The earlier backlog claim that dropping a proof header would remove a `check` refusal is unsupported. `check --message-id` never makes a request or sends a proof header: before this change, B reached `message_not_cached` on an unbound profile. The source path for a fresh `check` also uses the read edge, where no session proof is required. The comparison mutation above is the control that actually exercises this release's B refusal.

## Tests and mutation control

- The item I test covers A reaching a fake fetcher, B refusing before a deliberately damaged credential and cache, the missing-ID refusal, the old format and `manual`, setup writing A's binding and refusing a rebind before fetch, setup requiring an explicit host choice and ending repair failures with the operator step, hook stdin A/B/missing behavior, MCP startup and error entries, and every `--profile`-accepting row generated from `AGENT_COMMANDS`. The table yields 42 accepting rows; each B invocation refused at profile open. The test lives under `tests/p1-cli/`, which `test:p1-cli` globs.
- Mutation control: replacing the one comparison with `if (false)` made the B-refusal test fail (exit 1). It reached `check_state_invalid` from the damaged cache instead of `profile_other_session`. The comparison was restored and the same test passed (exit 0).
- The required `npm run build` and `npm run check:tests` passed. A focused run of item I, command-table gates, connection-token, and permission-default tests passed **42/42**; the item I file alone passed **7/7**. The connect component test passed **11/11**. Each had a temporary HOME and a 120-second process timeout.
- The required `env -u FORCE_COLOR npm test` and `env -u FORCE_COLOR npm run test:p1-cli` were run with a temporary HOME and a 360-second timeout per command. Both exited 1 amid sandbox `EPERM` errors for local Unix socket, loopback bind, and spawn operations. These broad runs do not establish a passing full suite here; the focused tests above passed. A site build was not run because its `sync:installer` step writes outside this lane's allowed paths.

## Not established in this lane

- No production host was contacted; the lead owns the production A/B/no-ID request-count control. No npm release or deployment was made.
- This local guard does not stop another process under the same OS user from reading or changing the profile and replaying A's ID. Existing unbound profiles are not migrated. Server-side read proof was not added.
- The host's static MCP configuration has not been shown to expand a fresh session ID into startup arguments.

## Fold 1 (2026-09-24)

The pre-edit item I suite passed 7/7 at `5fe61316`, including an assertion that expected B's noisy hook output. That suite did not prove the reviewed acceptance criteria. The review findings below were checked against local source and temporary profiles; all ten rulings needed a change. The earlier broad statement “No host variable is read by the CLI” is retired: host detection reads `CURSOR_AGENT`, `SAND_HOST_PORT`, and `CURSOR_AGENT_SOCKET`, and CLI code reads `CLAUDE_CONFIG_DIR`. cswarm reads no environment variable for the session id.

| Ruling | Change and acceptance test | Reversion mutation result |
|---|---|---|
| F1 | The project hook silently ignores stdin session ids other than its configured id, including an absent id. `hook stdin presents its own session id` checks B's empty output and unchanged error file around A's turn. | Restoring the original stdin binding check killed the test: B spoke and wrote a diagnostic. |
| F2 | `listen` and `session` profile handlers accept the retained host flag; session status/stop also accept the expanded workspace id, and human session lifecycle rows accept expanded credential flags. The generated `AGENT_COMMANDS` test runs every profile row with B and A; B refuses and A passes argument parsing. | Removing `host-session-id` from `listen start` killed the generated A control with `unknown option`. |
| F3 | Receive configure opens the profile with the raw optional id before synthesizing `manual`, including hook providers. `receive configure keeps an omitted id distinct from manual` pins both the code and the brief's sentence. | Passing synthesized `manual` killed the test with `profile_other_session`. |
| F4 | Hook failure, resume, setup next action, receive next actions, and channel reply instructions carry the saved id; static skill, guide, and connect examples show the flag. `bound command producers include the saved id; manual commands stay unchanged` checks the enumerable producers and static copy; the site observer checks the connect prompt. | Removing the hook failure command's id killed the producer test. |
| F5 | Guidance and the brief use the narrower session-id claim; the brief retains decision 4's retired wording in a dated correction. `src never reads an environment variable as the session id` walks the TypeScript AST, with `CLAUDE_CONFIG_DIR` as a positive control; the producer test pins the corrected guide and brief. | Restoring the broad guide claim killed the producer test. |
| F6 | The expand path still opens the profile before the credential. `expand checks B before a damaged credential` gets the other-session sentence for B and a credential error for A. | Reading the damaged credential before `readAgentProfile` killed the B assertion. |
| F7 | Setup reports `host_session_bound` and permits an owner to bind a manual or legacy profile with the connection file. `setup reports the resulting binding` checks true, false, and manual-to-bound. | Forcing the result false killed the bound assertion. |
| F8 | Receive updates check the profile before taking a lock. `receive test and idle leave B's profile folder byte-identical` compares files and holds B's lock to prove refusal occurs before lock acquisition. | Removing the pre-lock check killed the test with a lock timeout instead of the profile refusal. |
| F9 | Non-`AgentSetupError` setup failures append the operator step; binding codes retain their own sentences. `setup network failures include the operator step` uses a local refused port. | Removing the generic wrapper killed the step assertion. |
| F10 | MCP maps `profile_other_session` to stop and tell the operator, and `host_session_required` to restart this MCP server with the current host session. The item I and MCP table tests pin both. | Restoring the old other-session remedy killed the item I assertion. |

### After-change local evidence

- Temporary HOME and profiles; no real workspace. `npm run build` and `npm run check:tests` passed. The item I suite passed **13/13**. Its generated table enumerated all **42** profile-accepting rows and checked B refusal plus A argument acceptance.
- The required four-file command ran with a temporary HOME: **49/51 passed**. The onboarding HTTP-fixture case could not bind `127.0.0.1` (`EPERM`), and the MCP stdio test process hit a Node native assertion after its first two tests. The isolated MCP error-table test passed **1/1**. The site connect observer passed **11/11**. These failures do not establish the remaining integration behavior.
- Ten one-at-a-time source reversions were restored after their tests; **F1–F10 each failed its named acceptance test**. The credential-before-check and transient-lock mutations were among them. A clean rebuild and item I rerun passed after restoration.

### Deployment and live verification

None. No production host, staging host, server, migration, real workspace, deployment, commit, merge, or push was touched. Production request counts, live host hook ID equality, MCP startup with a host's static config, and the blocked HTTP/MCP integration cases remain unestablished.

## Fold 2 (2026-09-24)

### Before and after acceptance evidence

- Before the edit, the new human-session test found `session enable` in `expand` mode; the generated profile test counted 42 accepting rows. `listen start --host-session-id session-A` reached its credential requirement without a profile. The unbound resume probe initially needed a temporary `HOME` to keep hook locks inside the fixture; with that fixture corrected, its revert mutation showed the missing session B flag. The typed setup probe used a local renewal rejection, so no service request was made.
- After the edit, all three human-only variants refuse `--profile` and reject `--agent-token-file` before human login. The generated profile count is 39. All six selected listener/session control rows give a usage error for `--host-session-id` without `--profile`. Resume uses the receive binding for an unbound profile and the profile id for a bound one. A typed renewal error from setup keeps the CLI's non-JSON renewal formatting and ends with the operator step.

| Ruling | Change and acceptance test | Reversion mutation result |
|---|---|---|
| G1 | `session enable`, `disable`, and `recover` use `REFUSE_PROFILE`, expose only human flags plus `principal-id`, and restore the `0260ab8d` assertion shape. `human session lifecycle refuses agent credentials before human login` checks all three profile refusals and unknown agent-token-file errors with a temporary HOME and explicit local test target. The generated all-profile test now counts 39. | Adding `CREDENTIAL_FLAGS` back to the human assertion shape made the token-file refusal fail; it reached `not logged in; run cswarm login` instead of `unknown option`. Exit 1. |
| G2 | Resume's instruction uses `profile.host_session_id ?? binding?.host_session_id`. `resume uses receive binding for unbound profile and profile binding for bound profile` checks B for the unbound receive binding and A for the bound profile. | Reverting the fallback removed `--host-session-id 'session-B'` from the unbound instruction. Exit 1. |
| G3 | The parser remembers whether `--profile` was supplied before expansion. `listen start/status/stop/canary` and `session status/stop` reject a host id without it through `UsageError`. `host session id on listener and session control rows requires profile` checks the six declared rows and usage output. | Removing the `listen start` guard made that row reach its credential requirement instead of the usage refusal. Exit 1. |
| G4 | For non-`AgentSetupError` failures, setup adds the step to the original error message and rethrows that object. `setup preserves a typed renewal failure and CLI non-JSON handling` uses a temporary connection and a local renewal rejection; it checks the renewal message, operator step, and class-specific output without a `cswarm:` error prefix. | Restoring the plain `Error` wrapper made the same typed failure print as generic `cswarm:` output. Exit 1. |

### Tests

- Required `npm run build` and `npm run check:tests` passed. The required focused command passed 30/30 after the citation test's three line pointers were moved to the current `src/cli.ts` lines. All Item I tests have explicit timeouts; CLI child processes have a three-second timeout and temporary HOME.
- Each mutation was built and run against its named focused test with a 30-second process timeout. The original sources were restored after each run, and the final restored build passed.
- The additional dispatch-baseline test could not start its loopback fixture in this sandbox: `listen EPERM ... 127.0.0.1`. Its baseline was not regenerated or claimed passing here.

### Deployment and live verification

None. No production or staging host, real workspace, deployment, commit, merge, or push was touched. Production request counts, live hook session-id equality, static MCP argument behavior, and the full suites remain unestablished.

## Fold 3 (2026-09-24)

| Ruling | Change and acceptance test | Reversion mutation result |
|---|---|---|
| H1 (Grok FAIL; Opus minor 1) | `AGENT_PROFILE_COMMANDS` now reads accepting command pairs from `AGENT_COMMANDS`, keeping `profileListOrder`. A group appears by root only when every pair accepts `--profile`; the mixed `session` group appears as `session start`, `session status`, and `session stop`. `profile refusal names exactly the accepting command pairs in table order` derives the expected set from the table, checks that each named root or pair accepts the flag, and checks the CLI refusal text. | Mutating the producer to name the whole `session` group made the named test fail (exit 1); restored test passed. |
| H2 (Opus minor 2) | `session status` and `session stop` allow the expanded `workspace-id` only when the caller supplied `--profile`. Without it, the earlier `0260ab8d` `unknown option: --workspace-id` refusal is restored. `session status and stop reject workspace id without profile` checks both refusals, both no-profile positive controls reaching the credential requirement, and both profile expansions passing argument shape. | Mutating both handlers to accept `workspace-id` unconditionally made the named test fail (exit 1); restored test passed. |

### Tests and dispatch baseline

- `npm run build` and `npm run check:tests` passed after restoration. The Item I file passed **20/20** with temporary profile fixtures and a sanitized Cloud target environment. Each test declares a timeout; spawned CLI calls have a three-second timeout. The two mutation builds passed; each named mutant test failed as required.
- `UPDATE_DISPATCH_BASELINE=1 node --import tsx --test tests/p1-cli/command-dispatch-baseline.test.ts` passed twice (**1/1** each), with a temporary HOME per fixture and a loopback target. The two generated JSON files were byte identical (`cmp` exit 0; SHA-256 `e0ccc90751e45bfdd1e0f4f4d66122d3d9148ec30f3747c332315e3bb53cf9eb`). The counts file did not change.
- Exactly **273 of 1,261** baseline rows changed: 213 `refusal`, 39 `policy`, and 21 `selected-error`. In every changed row, only `stderr` changed, replacing the broad `session` claim with `session start, session status, session stop`. H2 changed no existing baseline row; its focused test pins both command pairs. Every changed row ID follows.

**refusal (213 rows):**

- `refusal.unknown-verb.profile-valid`, `refusal.unknown-verb.profile-missing`, `refusal.prototype-verb.constructor.profile-valid`, `refusal.prototype-verb.__defineGetter__.profile-valid`, `refusal.prototype-verb.__defineSetter__.profile-valid`, `refusal.prototype-verb.hasOwnProperty.profile-valid`
- `refusal.prototype-verb.__lookupGetter__.profile-valid`, `refusal.prototype-verb.__lookupSetter__.profile-valid`, `refusal.prototype-verb.isPrototypeOf.profile-valid`, `refusal.prototype-verb.propertyIsEnumerable.profile-valid`, `refusal.prototype-verb.toString.profile-valid`, `refusal.prototype-verb.valueOf.profile-valid`
- `refusal.prototype-verb.__proto__.profile-valid`, `refusal.prototype-verb.toLocaleString.profile-valid`, `refusal.group.hook.missing.profile-valid`, `refusal.group.hook.missing.profile-valid-json`, `refusal.group.hook.missing.profile-missing`, `refusal.group.hook.missing.profile-missing-json`
- `refusal.group.hook.unknown.profile-valid`, `refusal.group.hook.unknown.profile-valid-json`, `refusal.group.hook.unknown.profile-missing`, `refusal.group.hook.unknown.profile-missing-json`, `refusal.group.invite.missing.profile-valid`, `refusal.group.invite.missing.profile-valid-json`
- `refusal.group.invite.missing.profile-missing`, `refusal.group.invite.missing.profile-missing-json`, `refusal.group.invite.unknown.profile-valid`, `refusal.group.invite.unknown.profile-valid-json`, `refusal.group.invite.unknown.profile-missing`, `refusal.group.invite.unknown.profile-missing-json`
- `refusal.group.member.missing.profile-valid`, `refusal.group.member.missing.profile-valid-json`, `refusal.group.member.missing.profile-missing`, `refusal.group.member.missing.profile-missing-json`, `refusal.group.member.unknown.profile-valid`, `refusal.group.member.unknown.profile-valid-json`
- `refusal.group.member.unknown.profile-missing`, `refusal.group.member.unknown.profile-missing-json`, `refusal.group.workspace.missing.profile-valid`, `refusal.group.workspace.missing.profile-valid-json`, `refusal.group.workspace.missing.profile-missing`, `refusal.group.workspace.missing.profile-missing-json`
- `refusal.group.workspace.unknown.profile-valid`, `refusal.group.workspace.unknown.profile-valid-json`, `refusal.group.workspace.unknown.profile-missing`, `refusal.group.workspace.unknown.profile-missing-json`, `refusal.group.target.missing.profile-valid`, `refusal.group.target.missing.profile-valid-json`
- `refusal.group.target.missing.profile-missing`, `refusal.group.target.missing.profile-missing-json`, `refusal.group.target.unknown.profile-valid`, `refusal.group.target.unknown.profile-valid-json`, `refusal.group.target.unknown.profile-missing`, `refusal.group.target.unknown.profile-missing-json`
- `refusal.group.principal.missing.profile-valid`, `refusal.group.principal.missing.profile-valid-json`, `refusal.group.principal.missing.profile-missing`, `refusal.group.principal.missing.profile-missing-json`, `refusal.group.principal.unknown.profile-valid`, `refusal.group.principal.unknown.profile-valid-json`
- `refusal.group.principal.unknown.profile-missing`, `refusal.group.principal.unknown.profile-missing-json`, `refusal.group.token.missing.profile-valid`, `refusal.group.token.missing.profile-valid-json`, `refusal.group.token.missing.profile-missing`, `refusal.group.token.missing.profile-missing-json`
- `refusal.group.token.unknown.profile-valid`, `refusal.group.token.unknown.profile-valid-json`, `refusal.group.token.unknown.profile-missing`, `refusal.group.token.unknown.profile-missing-json`, `refusal.group.grant.missing.profile-valid`, `refusal.group.grant.missing.profile-valid-json`
- `refusal.group.grant.missing.profile-missing`, `refusal.group.grant.missing.profile-missing-json`, `refusal.group.grant.unknown.profile-valid`, `refusal.group.grant.unknown.profile-valid-json`, `refusal.group.grant.unknown.profile-missing`, `refusal.group.grant.unknown.profile-missing-json`
- `refusal.group.link.missing.profile-valid`, `refusal.group.link.missing.profile-valid-json`, `refusal.group.link.missing.profile-missing`, `refusal.group.link.missing.profile-missing-json`, `refusal.group.link.unknown.profile-valid`, `refusal.group.link.unknown.profile-valid-json`
- `refusal.group.link.unknown.profile-missing`, `refusal.group.link.unknown.profile-missing-json`, `refusal.prototype-sub-action.hook.constructor.profile-valid`, `refusal.prototype-sub-action.hook.__defineGetter__.profile-valid`, `refusal.prototype-sub-action.hook.__defineSetter__.profile-valid`, `refusal.prototype-sub-action.hook.hasOwnProperty.profile-valid`
- `refusal.prototype-sub-action.hook.__lookupGetter__.profile-valid`, `refusal.prototype-sub-action.hook.__lookupSetter__.profile-valid`, `refusal.prototype-sub-action.hook.isPrototypeOf.profile-valid`, `refusal.prototype-sub-action.hook.propertyIsEnumerable.profile-valid`, `refusal.prototype-sub-action.hook.toString.profile-valid`, `refusal.prototype-sub-action.hook.valueOf.profile-valid`
- `refusal.prototype-sub-action.hook.__proto__.profile-valid`, `refusal.prototype-sub-action.hook.toLocaleString.profile-valid`, `refusal.prototype-sub-action.invite.constructor.profile-valid`, `refusal.prototype-sub-action.invite.__defineGetter__.profile-valid`, `refusal.prototype-sub-action.invite.__defineSetter__.profile-valid`, `refusal.prototype-sub-action.invite.hasOwnProperty.profile-valid`
- `refusal.prototype-sub-action.invite.__lookupGetter__.profile-valid`, `refusal.prototype-sub-action.invite.__lookupSetter__.profile-valid`, `refusal.prototype-sub-action.invite.isPrototypeOf.profile-valid`, `refusal.prototype-sub-action.invite.propertyIsEnumerable.profile-valid`, `refusal.prototype-sub-action.invite.toString.profile-valid`, `refusal.prototype-sub-action.invite.valueOf.profile-valid`
- `refusal.prototype-sub-action.invite.__proto__.profile-valid`, `refusal.prototype-sub-action.invite.toLocaleString.profile-valid`, `refusal.prototype-sub-action.member.constructor.profile-valid`, `refusal.prototype-sub-action.member.__defineGetter__.profile-valid`, `refusal.prototype-sub-action.member.__defineSetter__.profile-valid`, `refusal.prototype-sub-action.member.hasOwnProperty.profile-valid`
- `refusal.prototype-sub-action.member.__lookupGetter__.profile-valid`, `refusal.prototype-sub-action.member.__lookupSetter__.profile-valid`, `refusal.prototype-sub-action.member.isPrototypeOf.profile-valid`, `refusal.prototype-sub-action.member.propertyIsEnumerable.profile-valid`, `refusal.prototype-sub-action.member.toString.profile-valid`, `refusal.prototype-sub-action.member.valueOf.profile-valid`
- `refusal.prototype-sub-action.member.__proto__.profile-valid`, `refusal.prototype-sub-action.member.toLocaleString.profile-valid`, `refusal.prototype-sub-action.workspace.constructor.profile-valid`, `refusal.prototype-sub-action.workspace.__defineGetter__.profile-valid`, `refusal.prototype-sub-action.workspace.__defineSetter__.profile-valid`, `refusal.prototype-sub-action.workspace.hasOwnProperty.profile-valid`
- `refusal.prototype-sub-action.workspace.__lookupGetter__.profile-valid`, `refusal.prototype-sub-action.workspace.__lookupSetter__.profile-valid`, `refusal.prototype-sub-action.workspace.isPrototypeOf.profile-valid`, `refusal.prototype-sub-action.workspace.propertyIsEnumerable.profile-valid`, `refusal.prototype-sub-action.workspace.toString.profile-valid`, `refusal.prototype-sub-action.workspace.valueOf.profile-valid`
- `refusal.prototype-sub-action.workspace.__proto__.profile-valid`, `refusal.prototype-sub-action.workspace.toLocaleString.profile-valid`, `refusal.prototype-sub-action.target.constructor.profile-valid`, `refusal.prototype-sub-action.target.__defineGetter__.profile-valid`, `refusal.prototype-sub-action.target.__defineSetter__.profile-valid`, `refusal.prototype-sub-action.target.hasOwnProperty.profile-valid`
- `refusal.prototype-sub-action.target.__lookupGetter__.profile-valid`, `refusal.prototype-sub-action.target.__lookupSetter__.profile-valid`, `refusal.prototype-sub-action.target.isPrototypeOf.profile-valid`, `refusal.prototype-sub-action.target.propertyIsEnumerable.profile-valid`, `refusal.prototype-sub-action.target.toString.profile-valid`, `refusal.prototype-sub-action.target.valueOf.profile-valid`
- `refusal.prototype-sub-action.target.__proto__.profile-valid`, `refusal.prototype-sub-action.target.toLocaleString.profile-valid`, `refusal.prototype-sub-action.principal.constructor.profile-valid`, `refusal.prototype-sub-action.principal.__defineGetter__.profile-valid`, `refusal.prototype-sub-action.principal.__defineSetter__.profile-valid`, `refusal.prototype-sub-action.principal.hasOwnProperty.profile-valid`
- `refusal.prototype-sub-action.principal.__lookupGetter__.profile-valid`, `refusal.prototype-sub-action.principal.__lookupSetter__.profile-valid`, `refusal.prototype-sub-action.principal.isPrototypeOf.profile-valid`, `refusal.prototype-sub-action.principal.propertyIsEnumerable.profile-valid`, `refusal.prototype-sub-action.principal.toString.profile-valid`, `refusal.prototype-sub-action.principal.valueOf.profile-valid`
- `refusal.prototype-sub-action.principal.__proto__.profile-valid`, `refusal.prototype-sub-action.principal.toLocaleString.profile-valid`, `refusal.prototype-sub-action.token.constructor.profile-valid`, `refusal.prototype-sub-action.token.__defineGetter__.profile-valid`, `refusal.prototype-sub-action.token.__defineSetter__.profile-valid`, `refusal.prototype-sub-action.token.hasOwnProperty.profile-valid`
- `refusal.prototype-sub-action.token.__lookupGetter__.profile-valid`, `refusal.prototype-sub-action.token.__lookupSetter__.profile-valid`, `refusal.prototype-sub-action.token.isPrototypeOf.profile-valid`, `refusal.prototype-sub-action.token.propertyIsEnumerable.profile-valid`, `refusal.prototype-sub-action.token.toString.profile-valid`, `refusal.prototype-sub-action.token.valueOf.profile-valid`
- `refusal.prototype-sub-action.token.__proto__.profile-valid`, `refusal.prototype-sub-action.token.toLocaleString.profile-valid`, `refusal.prototype-sub-action.grant.constructor.profile-valid`, `refusal.prototype-sub-action.grant.__defineGetter__.profile-valid`, `refusal.prototype-sub-action.grant.__defineSetter__.profile-valid`, `refusal.prototype-sub-action.grant.hasOwnProperty.profile-valid`
- `refusal.prototype-sub-action.grant.__lookupGetter__.profile-valid`, `refusal.prototype-sub-action.grant.__lookupSetter__.profile-valid`, `refusal.prototype-sub-action.grant.isPrototypeOf.profile-valid`, `refusal.prototype-sub-action.grant.propertyIsEnumerable.profile-valid`, `refusal.prototype-sub-action.grant.toString.profile-valid`, `refusal.prototype-sub-action.grant.valueOf.profile-valid`
- `refusal.prototype-sub-action.grant.__proto__.profile-valid`, `refusal.prototype-sub-action.grant.toLocaleString.profile-valid`, `refusal.prototype-sub-action.link.constructor.profile-valid`, `refusal.prototype-sub-action.link.__defineGetter__.profile-valid`, `refusal.prototype-sub-action.link.__defineSetter__.profile-valid`, `refusal.prototype-sub-action.link.hasOwnProperty.profile-valid`
- `refusal.prototype-sub-action.link.__lookupGetter__.profile-valid`, `refusal.prototype-sub-action.link.__lookupSetter__.profile-valid`, `refusal.prototype-sub-action.link.isPrototypeOf.profile-valid`, `refusal.prototype-sub-action.link.propertyIsEnumerable.profile-valid`, `refusal.prototype-sub-action.link.toString.profile-valid`, `refusal.prototype-sub-action.link.valueOf.profile-valid`
- `refusal.prototype-sub-action.link.__proto__.profile-valid`, `refusal.prototype-sub-action.link.toLocaleString.profile-valid`, `refusal.profile.__listen-supervisor`, `refusal.profile.login`, `refusal.profile.logout`, `refusal.profile.invite`
- `refusal.profile.member`, `refusal.profile.workspace`, `refusal.profile.target`, `refusal.profile.status`, `refusal.profile.workspaces`, `refusal.profile.use`
- `refusal.profile.new`, `refusal.profile.accept`, `refusal.profile.principal`, `refusal.profile.token`, `refusal.profile.grant`, `refusal.profile.link`
- `refusal.profile.command`, `refusal.profile.dogfood`, `refusal.profile.seed-fixture`

**policy (39 rows):**

- `policy.host-session.__listen-supervisor.drop`, `policy.host-session.accept.drop`, `policy.host-session.command.drop`, `policy.host-session.dogfood.drop`, `policy.host-session.grant.refusal.drop`, `policy.host-session.grant.resume.drop`
- `policy.host-session.hook.install.drop`, `policy.host-session.hook.refusal.drop`, `policy.host-session.hook.uninstall.drop`, `policy.host-session.invite.create.drop`, `policy.host-session.invite.refusal.drop`, `policy.host-session.invite.revoke.drop`
- `policy.host-session.link.new.drop`, `policy.host-session.link.refusal.drop`, `policy.host-session.link.revoke.drop`, `policy.host-session.login.drop`, `policy.host-session.logout.drop`, `policy.host-session.member.refusal.drop`
- `policy.host-session.member.remove.drop`, `policy.host-session.new.drop`, `policy.host-session.principal.create.drop`, `policy.host-session.principal.refusal.drop`, `policy.host-session.principal.revoke.drop`, `policy.host-session.seed-fixture.drop`
- `policy.host-session.session.disable.drop`, `policy.host-session.session.enable.drop`, `policy.host-session.session.recover.drop`, `policy.host-session.status.drop`, `policy.host-session.target.clear.drop`, `policy.host-session.target.refusal.drop`
- `policy.host-session.target.set.drop`, `policy.host-session.target.show.drop`, `policy.host-session.token.mint.drop`, `policy.host-session.token.refusal.drop`, `policy.host-session.token.revoke.drop`, `policy.host-session.use.drop`
- `policy.host-session.workspace.close.drop`, `policy.host-session.workspace.refusal.drop`, `policy.host-session.workspaces.drop`

**selected-error (21 rows):**

- `selected-error.status.profile-valid-before`, `selected-error.status.profile-missing-before`, `selected-error.status.json-profile-valid-before`, `selected-error.status.json-profile-missing-before`, `selected-error.status.profile-json-valid-before`, `selected-error.status.profile-json-missing-before`
- `selected-error.status.host-before`, `selected-error.use.profile-valid-before`, `selected-error.use.profile-missing-before`, `selected-error.use.json-profile-valid-before`, `selected-error.use.json-profile-missing-before`, `selected-error.use.profile-json-valid-before`
- `selected-error.use.profile-json-missing-before`, `selected-error.use.host-before`, `selected-error.workspaces.profile-valid-before`, `selected-error.workspaces.profile-missing-before`, `selected-error.workspaces.json-profile-valid-before`, `selected-error.workspaces.json-profile-missing-before`
- `selected-error.workspaces.profile-json-valid-before`, `selected-error.workspaces.profile-json-missing-before`, `selected-error.workspaces.host-before`

### Not established

No production or staging host, real workspace, server, migration, deployment, commit, merge, or push was touched. These local tests do not establish production request counts, live host hook ID equality, or static MCP argument behavior. Full service and site suites, CI, and independent review of this uncommitted Fold 3 patch were not run.
