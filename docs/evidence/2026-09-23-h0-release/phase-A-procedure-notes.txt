Procedure problems: (1) Preflight grep -Fx prints the matched gate lines to stdout, so PASS lines duplicate. (2) Text says transfer proofs after PROOF_DIR exists; the Mac block runs before the box creates it. (3) Unpack waits for HezLead to confirm the proof list has no secrets; this brief never did that confirm, and the list was only required-edge-env.json and edge-env-source-check.txt. (4) Mac tar injected LIBARCHIVE.xattr.com.apple.provenance and AppleDouble ._ files into the proof archive. (5) sha256sum --check prints every path; huge verify log. (6) Inventory required names from REQUIRED_MAIN_ENV are SUPABASE_ANON_KEY, SUPABASE_SERVICE_ROLE_KEY, SUPABASE_URL, SWARM_DATABASE_URL|SUPABASE_DB_URL, SWARM_SELF_SERVE. That is the router base, not the h0-minus-service-role note. (7) Section 5 says start a backup if freshness fails; this window said stop instead. (8) Gate evidence must exist before preflight even though preflight creates EVIDENCE_DIR. (9) Box blocks assume an interactive root shell; I ran them as sudo -n -i bash -s, which the intro allows as a script. (10) KIND_LIST=%q stores edge\ stack; it sources correctly.

Stopped here. Did not run section 5 migrations or section 6.
